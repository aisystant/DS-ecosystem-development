---
type: work-package
number: 73
title: "Архитектурный план ИТ-платформы Aisystant"
status: in_progress
budget: "10-15h (~5-7 сессий)"
created: 2026-02-26
deadline: W14
repo: DS-ecosystem-development
artifact: "Архитектурный план: as-is, to-be, gap-анализ, ЭМОГССБ, ADR, roadmap"
source: "WP-65 (IWE Web App) + WP-24 (ЦД) + стратегия платформы"
---

# РП #73: Архитектурный план ИТ-платформы Aisystant

<details>
<summary><b>1. Контекст</b></summary>

### 1.1. Назначение документа

Документ **«Архитектурный план Aisystant v2»** — зонтичный РП, координирующий WP-65 (IWE Web App MVP), WP-24 (ЦД) и новые направления (биллинг, лояльность, ORY-миграция). Включает:

1. **As-is:** полная карта текущей платформы (все системы, репо, интеграции)
2. **To-be:** целевая архитектура (Web App + ЦД + биллинг + лояльность + ИИ-системы)
3. **Gap-анализ:** что есть → что нужно → что мигрировать
4. **ЭМОГССБ-оценка** целевой архитектуры (+ Стоимость)
5. **ADR:** ключевые архитектурные решения
6. **Roadmap:** фазы и приоритеты развития

### 1.2. Текущая платформа

Платформа Aisystant — ядро экосистемы развития интеллекта. Текущая архитектура: **Discourse (клуб) + LMS Aisystant (обучение) + Telegram-бот (быстрый канал)**. Назрела потребность в новой архитектуре с Web App как каноническим интерфейсом, цифровым двойником, биллингом, программой лояльности и ИИ-системами.

### 1.3. Концепция использования (→ WP-74)

> Роли, сценарии, тиры и UX-принципы вынесены в отдельный документ: **[WP-74 — Концепция использования ИТ-платформы Aisystant](WP-74-platform-concept-of-use.md)**

Содержание WP-74:

- **Миссия:** цифровая среда для развёртывания Интеллектуальной рабочей среды (IWE). Цивилизационная амбиция: фундаментальное образование мирового уровня для миллионов ($10 и 10 часов в неделю)
- **9 платформенных ролей (люди):** Участник сообщества, Автор руководств, Администратор, Бухгалтер, Маркетолог, Наставник, Тех.оператор, Амбассадор, Админ (владелец)
- **Участник — два независимых измерения:** тир (подписка, что ДОСТУПНО: T1–T4) и ролевая траектория (фокус ВНИМАНИЯ: Ученик → Интеллектуал → Профессионал → Исследователь → Просветитель). Измерения не привязаны друг к другу
- **ИИ-роли (агенты):** Проводник, Консультант, ДЗ-чекер, Стратег, Экстрактор, Оценщик, Модератор клуба, Публикатор, Командный координатор
- **11 сценариев (SC-1 → SC-11):** онбординг, обучение, целевые системы, ЦД-персонализация, баллы и репутация, сообщество, мультиповерхностный доступ, командообразование, масштабирование (Global Core + Local Edge), управление потоками и наставничество, назначение на позицию
- **Тиры:** T0 (бэк-офис: CRM-оператор, оператор потоков, администратор доступов, куратор), T1–T4 (учащиеся: Старт → Изучение → Персонализация → Созидание), TM1–TM3 (наставники: стажёр → наставник → старший), T5 (владелец — суперпозиция)
- **UX:** «банковское приложение для развития» + дашборды по ролям (учащийся, наставник, администратор, куратор)
- **Сводные матрицы:** сценарии × роли, сценарии × предметные области × системные уровни

### 1.4. Связанные РП

| РП | Связь |
|----|-------|
| #65 | IWE Web App MVP — основной драйвер и первая реализация |
| #24 | Цифровой двойник — ядро данных платформы |
| #5 | Бот: развитие — один из каналов (surface) |
| #40 | Экзокортекс шаблон — шаблон для пользователей |
| #63 | Терминология — единый язык платформы (§1.5) |
| #74 | Концепция использования — кто, зачем и как (§1.3) |
| **#109** | **Activity Hub v2 — реализация платформенного журнала событий + Event Bus (ADR-009)** |

### 1.5. Терминология платформы

> **Источник:** WP-63 (Стандарт терминологии) + DP.D.033 (Role-centric архитектура). Здесь — рабочий реестр терминов, применяемых в этом документе.

| Термин (UL) | Определение | НЕ путать с |
|-------------|-------------|-------------|
| **Роль (Role)** | Функция в системе: Стратег (R1), Экстрактор (R2) и т.д. Исполняется человеком или ИИ-системой | Агент, Исполнитель |
| **Исполнитель (Holder)** | Тот, кто исполняет роль. Один исполнитель → несколько ролей. Claude = исполнитель роли Стратег (R1) | Роль |
| **ИИ-система** | Автономная программная система (бот, MCP-сервер, агентная система). НЕ синоним «ИИ-роли» | Агент (полисемичный), Роль |
| **Система** | Bounded context с чётким назначением, интерфейсами и данными | Подсистема, Модуль |
| **Сервис** | Описание метода = именованная точка входа (entry point). Каталог: DP.MAP.002 | Процесс (полисемичный) |
| **Поверхность (Surface)** | Thin client, интерфейс пользователя: Web App, бот, CLI | Система |
| **Тир (Tier)** | Ось Entitlement в модели доступов. Определяет «что ДОСТУПНО» по подписке | Роль (что можно ДЕЛАТЬ) |
| **Позиция (Preset)** | Именованный бандл: Role + Tier + Scope. Назначается одним действием | Должность (у нас сообщество, не компания) |

> **Связь с WP-63:** Миграция ID `DP.AGENT.xxx → DP.ROLE.xxx` завершена (WP-63 done, 2026-03-10). В документе используется корректная терминология: «роль», а не «агент», для обозначения функций.

</details>
<details>
<summary><b>2. Текущее состояние (As-is)</b></summary>

### 2.1. Архитектура (3-слойная)

**Source:** `DP.ARCH.001-platform-architecture.md`

```
Layer 3: ИНТЕРФЕЙСЫ (thin clients)
  ├── Telegram Bot (@aist_me_bot)
  ├── Web LMS (aisystant.system-school.ru, Java/Vaadin)
  ├── Discourse (systemsworld.club)
  ├── Claude Code CLI (T4)
  └── [PLANNED] Web App (Next.js)

Layer 2: ОБРАБОТКА (два типа peer-to-peer)
  ├── Zone A: ИИ-системы (LLM, stateless)
  │   ├── Стратег (R1, Grade 3)
  │   ├── Экстрактор (R2, Grade 3)
  │   ├── Консультант (R3, Grade 2)
  │   ├── Проводник (R13, Grade 2)
  │   ├── Оценщик (R12, Grade 2)
  │   ├── Наладчик (R11, Grade 2)
  │   └── Статистик (R10, Grade 1)
  └── Zone B: Детерминированные (code, stateful, MCP)
      ├── Digital Twin (DS-twin, CF Workers, Ory OAuth)
      ├── Knowledge MCP (CF Workers, 5400+ docs, 9 sources)
      ├── Guides MCP (CF Workers, ~40 гайдов)
      ├── Composer MCP (бот-ассистент)
      ├── LMS Aisystant (Java, system-school.ru)
      ├── Billing [PLANNED]
      ├── ORY (identity, OAuth2) [MIGRATING]
      ├── CRM [PLANNED]
      └── Event Bus [PLANNED]

Layer 1: ДАННЫЕ + ИНФРАСТРУКТУРА
  ├── Neon PostgreSQL (pgvector, pg_trgm)
  ├── GitHub Repos (25+)
  ├── Railway (Python bot, EU Amsterdam)
  ├── Cloudflare Workers (MCP, global edge)
  └── Cache (Redis planned)
```

### 2.2. Карта систем (17 подсистем)

| # | Система | Статус | Назначение |
|---|---------|--------|-----------|
| 1 | Цифровой двойник (DS-twin, digital-twin-mcp) | 🟡 | MCP-интерфейс + Neon schemas |
| 2 | LMS и обучение (aisystant, system-school.ru) | ✅ | Java 8 / Vaadin 8 монолит |
| 3 | Клуб (Discourse, systemsworld.club) | ✅ | Сообщество, дискуссии |
| 4 | CRM | ❌ PLANNED | Customer journey, воронка |
| 5 | Биллинг и оплата | ❌ PLANNED | Подписки, feature gating |
| 6 | Централизованное хранилище (Knowledge MCP) | ✅ | 5400+ docs, 9 sources |
| 7 | Хаб активностей (Activity Hub) | ❌ PLANNED | Агрегатор событий |
| 8 | Проводник (Route Guide AI) | ❌ PLANNED | Маршрутизация по ЦД |
| 9 | MCP Hub (Registry + Gateway + Provisioner) | ❌ PLANNED | Каталог, маршрутизация и создание MCP (ADR-018). Поглощает Apps SDK |
| 10 | ORY (Identity & Access) | 🟡 MIGRATING | OAuth2, SSO |
| 11 | Proof-of-Impact (баллы) | ❌ PLANNED | Система баллов и лояльности |
| 12 | Эпистемический граф | ❌ PLANNED | Граф знаний/компетенций |
| 13 | Бот Aist (aist_bot_newarchitecture) | ✅ | Telegram-бот, быстрый канал |
| 14 | SystemsSchool_bot (стажировки) | ✅ | TG-бот расписаний |
| 15 | MCP-серверы (knowledge, guides, twin, composer) → MCP Hub | ✅ → 🟡 | Доступ к данным и знаниям. MCP Hub = единый endpoint + User/Community MCP (ADR-018) |
| 16 | ИИ-системы (DS-ai-systems, 7 ролей) | ✅ | Монорепо ИИ-систем |
| 17 | Публикатор (club publishing pipeline) | ✅ | Публикация в клуб |

### 2.3. Реестр репозиториев (22 активных)

**Source:** `DS-ecosystem-development/0.OPS/REPOSITORY-REGISTRY.md`

| # | Репо | Тип | Назначение |
|---|------|-----|-----------|
| 0 | ZP | Base/Принципы | Нулевые принципы (6 мета-ограничений) |
| 1 | FPF (ailev) | Base/Принципы | First Principles Framework |
| 2 | SPF | Base/Принципы | Second Principles Framework |
| 3 | FMT-S2R | Base/Форматы | Structured Second-level Repository |
| 14 | FMT-exocortex-template | Base/Форматы | Exocortex template (fork & deploy) |
| 4 | PACK-personal | Pack | Персональное развитие (224 entities) |
| 5 | PACK-ecosystem | Pack | Экосистема развития интеллекта |
| 6 | PACK-digital-platform | Pack | ИТ-платформа (213 entities, source-of-truth) |
| 20 | PACK-MIM | Pack | Мастерская (25 entities) |
| 24 | PACK-education | Pack | Методика обучения (43 entities) |
| 8 | DS-twin | DS/instrument | MCP-сервис ЦД (Cloudflare Workers) |
| 9 | DS-Knowledge-Index-Tseren | DS/instrument | Посты + публикации |
| 10 | DS-ecosystem-development | DS/governance | Координация экосистемы |
| 11 | DS-my-strategy | DS/governance | Личное стратегирование |
| 12 | docs (aisystant) | DS/surface | VitePress руководства (RU/EN) |
| 13 | DS-marathon-v2-tseren | DS/surface | Программа марафона |
| 15 | DS-ai-systems | DS/instrument | Монорепо 7 ИИ-систем |
| 18 | digital-twin-mcp | DS/instrument | MCP-сервер ЦД (Ory OAuth) |
| 19 | aist_bot_newarchitecture | DS/instrument | Telegram-бот (State Machine) |
| 21 | aisystant (external) | DS/instrument | LMS Aisystant (Java/Vaadin) |
| 22 | SystemsSchool_bot (external) | DS/instrument | TG-бот стажировок |
| — | DS-MCP | DS/instrument | knowledge-mcp + guides-mcp + composer |

### 2.4. Техстек ключевых систем

| Система | Язык | Фреймворк | DB | Deploy | Auth |
|---------|------|-----------|----|--------|------|
| **Aisystant LMS** | Java 8 + Groovy | Vaadin 8, Spring Data JPA, Hibernate | PostgreSQL + Solr 8.5 | WAR (Nomad?) | **Ory Network** + legacy Keycloak |
| **Aist Bot** | Python 3.11 | aiogram 3.20, asyncpg | Neon PostgreSQL | Railway (Docker) | TG auth, GitHub OAuth |
| **SystemsSchool Bot** | Python 3.13 | python-telegram-bot 22.0 | Нет (API calls) | **Vultr K8s** (Werf/Helm) | Aisystant API |
| **knowledge-mcp** | TypeScript | CF Workers | Neon + pgvector | Cloudflare Workers | Public |
| **digital-twin-mcp** | JavaScript | CF Workers | Neon + CF KV | Cloudflare Workers | **Ory OAuth2 (PKCE)** |
| **guides-mcp** | TypeScript | CF Workers | **SurrealDB** ⚠️ | Cloudflare Workers | — |
| **fsm-mcp** | TypeScript | CF Workers | — | Cloudflare Workers | — |
| **docs** | Node.js | VitePress | SurrealDB (publish) ⚠️ | VK Cloud VM + Nomad | — |

> ⚠️ **SurrealDB — отказ.** Guides-mcp и docs publishing используют SurrealDB. При миграции заменить на Neon PostgreSQL (единая БД экосистемы).

**Платёжные системы в LMS:** Stripe, YooKassa, Paybox, Ecwid, Tilda (все webhook). TG Stars — в боте.

**Ory Network:** Проект `thirsty-goldstine-5xjpbvdi2b.projects.oryapis.com`. Используется в LMS и digital-twin-mcp. Keycloak — legacy (только тесты).

### 2.5. Топология деплоя (DP.D.030)

| Компонент | Провайдер | Регион | Стоимость |
|-----------|-----------|--------|-----------|
| Бот (Python, aiogram) | Railway | EU Amsterdam | $5/мес |
| MCP-серверы (TypeScript) | Cloudflare Workers | Global (auto) | Free tier |
| DB (PostgreSQL + pgvector) | Neon | EU Frankfurt | Free → $20/мес |
| Код + шаблон | GitHub | — | Free |
| Руководства (VitePress) | VK Cloud VM | ? | ? |

**Решение архитектора (19 фев):** Текущая связка верна. Позже Railway → Kubernetes.

### 2.6. Тиры обслуживания (→ WP-74 §3)

> **Тиры = ось Entitlement** в трёхосевой модели доступов (ADR-014, P13). Тир определяет «что ДОСТУПНО», а не «что можно ДЕЛАТЬ» (это роль) и не «над чем» (это scope). Детальное описание тиров → [WP-74 §3](WP-74-platform-concept-of-use.md).

| Ось | Тиры | Для кого |
|-----|------|----------|
| **T0** | Бэк-офис / Администрирование | CRM-оператор, оператор потоков, администратор доступов, куратор |
| **T1–T4** | Старт → Изучение → Персонализация → Созидание | Учащиеся (траектория роста) |
| **TM1–TM3** | Стажёр → Наставник → Старший наставник | Наставники (дополнительно к T2+) |
| **T5** | Владелец платформы | Суперпозиция T0 + T4 + TM3 |

### 2.7. Идентификация (ORY)

**Статус:** ORY OAuth2 уже интегрирован в digital-twin-mcp (worker-sse.js):
- PKCE flow: `/authorize` → Ory `/oauth2/auth` → callback → token exchange
- ORY_PROJECT_URL, ORY_CLIENT_ID, ORY_CLIENT_SECRET в env
- ID token декодирование, refresh tokens хранятся в KV
- **Решение (29 мар):** self-hosted ORY в РФ + ORY Network EU. **Репликация инстансов** (не федерация)

**LMS Aisystant (Java):** Текущая авторизация через `aisystant.system-school.ru` (email + пароль), TG-привязка. Миграция на ORY — необходима для единого SSO.

### 2.8. Цифровой двойник (DP.ARCH.003)

> **Политика данных:** [DP.D.035 — Единая политика данных IWE](../../../PACK-digital-platform/pack/digital-platform/01-domain-contract/DP.D.035-data-policy.md) — что собирается, где хранится, кто имеет доступ, как удаляется. Агрегирует DP.D.028 (user data tiers), DP.D.031 (MCP access model), DP.ARCH.003 (digital twin architecture).

**Neon DB — 5 schemas (предложение для архитектора):**

| Schema | Назначение | Ключевые таблицы |
|--------|-----------|-----------------|
| `public` | User identity | users (canonical profile) |
| `development` | Growth: events + projections | user_events (append-only), skill_mastery (BKT), memory_decay (HLR), engagement, misconceptions, qualifications |
| `finance` | Billing, баллы, royalties | point_transactions, point_balances, subscriptions, payments, royalty_rules |
| `connections` | OAuth tokens, API keys | oauth_tokens (pgcrypto), api_keys, external_accounts |
| `operations` | FSM, cache, content, monitoring | fsm_states, content_cache, error_logs |

**Event Sourcing:** `user_events` таблица, 40+ типов событий, append-only, JSONB payload, confidence scores (0.0-1.0).

**5 проекций состояния:** skill_mastery (BKT), memory_decay (HLR), engagement, misconceptions, qualifications.

**MCP ЦД:** digital-twin-mcp (Cloudflare Workers), 3 инструмента: describe_by_path, read_digital_twin, write_digital_twin. **ORY OAuth2 уже интегрирован** (PKCE flow в worker-sse.js).

### 2.9. Биллинг и лояльность

**Source:** `DS-ecosystem-development/.../Описание системы биллинга 3.2.md`
**Тарифная политика:** [Тарифы и пакеты 2.1](../../../A.Systems-Builder/A2.Systems-Builder/A2.1.Meaning/2.1.1.%20Продуктовые%20предложения/Тарифы%20и%20пакеты%202.1.md) — подписка (T1-T4) + события (on-demand) + система баллов

**5 функций:**
1. Управление тарифами (freemium, subscription, pay-per-course, семейные/корпоративные)
2. Управление доступами (feature gating, quota, auto-отключение)
3. Система баллов (начисление за активные действия, оплата программных продуктов баллами)
4. Распределение доходов (Revenue Sharing: platform 30%, author 50%, instructor 15%, curator 5%) _(пожелание: детализировать пропорции — маркетинг ~10%, наставники+авторы суммарно ~50%; уточнить в спецификации Billing Phase 2)_
5. Финансовая отчётность (MRR, ARR, LTV, CAC, churn)

**Каналы оплаты (P12 — две юрисдикции, единый биллинг):**
- **Россия 🇷🇺:** YooKassa, Paybox, TG Stars, system-school.ru (подписка БР + отдельные курсы)
- **Мир 🌍:** Stripe, PayPal, TG Stars
- **Единый Billing Service** с адаптерами по юрисдикции (Strategy pattern). Подписки, тарифы, баллы — общие

**Система баллов (вместо скидок и токенов):**

Принцип: вместо фиксированных скидок по тирам — начисление баллов за активные действия. Баллами можно полностью или частично оплачивать программные продукты (практикумы, резидентуры, семинары).

Начисление (множитель по квалификации: T2 x1, T3 x1.5, T4 x2):
- Завершённый слот обучения: 5-10 баллов
- Завершённый модуль: 20-50 баллов
- Перенос в практику (артефакт): 20-100 баллов
- Peer-review чужой работы: 10-50 баллов
- Вклад в сообщество (пост, кейс): 10-30 баллов
- Проверка ДЗ (наставник): 5-20 баллов (фиксировано)
- Проект / рабочий продукт: 50-500 баллов

Трата (1 балл ≈ $1):
- Практикум ($300-500): 300-500 баллов
- Резидентура ($800-2000): 800-2000 баллов
- Семинар (₽5000-12000): 50-120 баллов
- Менторская сессия: 80-150 баллов

Ключевое: чем выше квалификация участника, тем больше множитель начисления. Это мотивирует апгрейд тира и активное участие. Баллы — признание вклада, а не покупаемая валюта.

### 2.9.1. Анализ процесса оплаты (as-is, 2026-03-16)

> **Источник:** верификация WP-73, Phase 2. Проверены: `handlers/buy.py`, `handlers/subscription.py`, `core/access.py`, `clients/aisystant.py`, `core/tier_detector.py`.

**Архитектура оплаты — pull-модель:**

```
Пользователь → /buy | /subscription
  → Бот создаёт платёж через Aisystant API (create_subscription_payment / create_internship_payment)
  → Aisystant возвращает confirmationUrl (YooKassa)
  → Пользователь переходит по ссылке и оплачивает
  → YooKassa → webhook → Aisystant LMS (Java) → запись подписки/доступа в БД Aisystant
  → Бот при следующем действии: has_active_subscription() → Aisystant API → True
  → Tier detection: T1 → T2 → UI обновляется
```

**Матрица автоматизации:**

| Ресурс | Авто? | Механизм | Проблемы |
|--------|:-----:|----------|----------|
| Подписка БР (доступ к боту) | ✅ | Pull: `has_active_subscription()` → кэш 5 мин | Задержка до 5 мин; нет уведомления «оплата прошла» |
| Доступ к LMS (курсы, видео) | ✅ | Aisystant обрабатывает webhook YooKassa → активирует подписку | Управляется Aisystant, бот не участвует |
| Курсы/программы | ✅ | `create_internship_payment()` → Aisystant активирует доступ к потоку | Бот не знает, прошла ли оплата курса |
| Видео | ✅ | Привязаны к курсам в LMS | Нет отдельной покупки видео через бот |
| Telegram-чаты | ⛔ | `approve_chat_join()` существует, но **не вызывается** после оплаты | **Не автоматизировано** — нет триггера |
| Бот (paid features) | ✅ | `AccessLayer.has_access()` → подписка + триал | Работает с задержкой кэша |

**Выявленные gaps (→ WP-115):**

1. **Нет push-уведомления после оплаты** — пользователь не получает сообщение «Оплата прошла! Вот что доступно»
2. **Telegram-чаты не выдаются автоматически** — нет маппинга «курс/подписка → TG-чаты»
3. **`grant_access()` и `check_purchase()` — стабы** (TODO Phase 4.3 в `access.py`)
4. **Нет webhook от Aisystant к боту** — только polling с кэшем 5 мин
5. **`check_payment()` не вызывается** — метод существует в `aisystant.py:341`, но ни один handler его не использует
6. **MAP.002 не содержит платёжных сервисов** — нет описания Billing, Fulfillment, Access Provisioning

### 2.10. Принятые архитектурные решения (до этого РП)

| # | Решение | Файл | Score |
|---|---------|------|-------|
| ADR-001 | **Multi-surface с Web-ядром (Next.js)** для международной аудитории | `01D-adr-web-ui-platform.md` | 69/80 |
| DP.ARCH.003 | **3-слойный ЦД:** Events → State → Views (Event Sourcing) | `DP.ARCH.003-digital-twin-architecture.md` | 52/60 (8.7) |
| DP.D.030 | **Топология:** Railway + CF Workers + Neon + GitHub → K8s | `DP.D.030-deployment-topology.md` | — |
| DP.D.031 | **MCP:** knowledge=публичный, digital-twin=приватный | `DP.D.031-mcp-access-model.md` | — |
| DP.D.033 | **Role-centric:** роль ≠ исполнитель | `DP.D.033-role-centric-architecture.md` | 50/60 |
| DP.D.035 | **Единая политика данных IWE:** сбор, хранение, доступ, удаление | `DP.D.035-data-policy.md` | — |
| Архитектор-19фев | Railway→K8s, Event-driven sync, Grafana Cloud, Webhook | `architect-questions-2026-02-20.md` | — |
| **Архитектор-29мар** | **RLS** (Q1), **Activity Hub = журнал всех событий, pg_notify** (Q4), **ORY репликация** не федерация (Q2), **ORY Keto** (Q5), **Vue + раздельные хостинги** (Q3), **LMS не трогаем** (Q7), Vercel KV нет (Q8) | `0.99.Archive/WP-73-architect-agenda.md` | — |

</details>
<details open>
<summary><b>3. Целевая архитектура (To-be)</b></summary>

### 3.1. Архитектурные принципы

> **Разделение по уровням** (hard-distinction #18, FPF):
> - 2-й уровень (доменные) → **source-of-truth: [DP.ARCH.001 §7](../../../PACK-digital-platform/pack/digital-platform/02-domain-entities/DP.ARCH.001-platform-architecture.md)** (20 принципов, с покрытием характеристик §7.1)
> - 3-й уровень (проектные) → ADR в этом документе (§4.3)
> - 1-й уровень (FPF) → ссылки, не дублируем

#### 3.1.1. Принципы 2-го уровня (из DP.ARCH.001 §7, source-of-truth)

20 доменных принципов, применимых к любой платформе экосистемы развития. Полный список с описаниями и покрытием характеристик → DP.ARCH.001 §7–7.1. Ключевые:

| DP.ARCH.001 # | Принцип | Защищает |
|---|---|---|
| #5 | Evolvability-first | Э |
| #6 | Отчуждаемость (platform-space vs user-space) | М, Г |
| #8 | Знания публичны, данные приватны (DP.D.031) | М, Б |
| #12 | Интерфейсы через обработку (Слой 3 → Слой 2 → Слой 1) | Э, Б |
| #13 | ИИ-системы UI-agnostic | Э, М |
| #14 | Event Sourcing для ЦД | Э, Б |
| #15 | Multi-surface (Web App = каноническая) | Э, Г |
| #17 | Три оси доступа: Entitlement ∩ Role ∩ Scope (DP.D.034) | Б, М |
| #20 | Integrate SOTA, don't reinvent | С(овр), С(кор) |
| #21 | **Экзоскелет, не протез** — ИИ усиливает мышление пользователя, а не заменяет. Предъявление знания > генерация; вопрос > ответ (в стратегических решениях); scaffolding убывает с ростом компетенции | О |

> **Принцип #21 «Экзоскелет, не протез» (утверждён в DP.ARCH.001 §7):** ИИ усиливает мышление пользователя, а не заменяет. Предъявление знания > генерация; вопрос > ответ (в стратегических решениях); scaffolding убывает с ростом компетенции. Защищает характеристику Обучаемость (О).
>
> **AI Self-Learning Flywheel** — вынесен из принципов 2-го уровня в проектный принцип 3-го уровня (P-SLF, §3.1.2). Концепция маховика самообучения ИИ на артефактах участников остаётся, но как проектное решение Aisystant, а не универсальный принцип платформы.

#### 3.1.1a. Ритм дня и сессия саморазвития (конкретизация P21)

> **WP-98.** ОРЗ-фрактал: паттерн Open → Work → Close применяется на двух масштабах — День и Сессия.

**Ритм дня (Day Rhythm):**

| Масштаб | Открытие | Работа | Закрытие |
|---------|----------|--------|----------|
| **День** | Вход в контекст дня (план, саморазвитие, мир) | Сессии с перерывами (помидорки) | Рефлексия: что узнал, похвала, задел |
| **Сессия** | WP Gate, определение роли | Capture, Pre-action Gates | Коммит, captures, статус |

**Сессия саморазвития** — специальный тип сессии ОРЗ:
- **Открытие:** агент показывает текущее руководство, где остановился, список черновиков
- **Работа:** мышление письмом, работа с руководством, черновики
- **Закрытие:** стандартный Close + captures в Pack
- **Связь с DayPlan:** Слот 1 = саморазвитие. Если нет — агент явно сообщает пользователю

**Требования к агенту (экзоскелет):**
- **Помидорки:** 25 мин работа → 5 мин перерыв. После 4 циклов — длинный перерыв 15-30 мин
- **Напоминание о перерыве:** если сессия >50 мин без перерыва → агент напоминает
- **«Что нового узнал»:** при закрытии дня агент подсвечивает captures, инсайты, новые знания — помогает пользователю увидеть свой прогресс (экзоскелет, не протез)
- **Похвала:** обязательный шаг закрытия дня — рефлексия достижений

**День стратегии:** Один день в неделю — сессия стратегирования (пользователь выбирает день). DayPlan этого дня содержит напоминание.

#### 3.1.2. Принципы 3-го уровня (проектные решения Aisystant)

> Специфичны для проекта Aisystant. Не переносятся в Pack.

| # | Принцип | Суть | ADR |
|---|---------|------|-----|
| P9 | **Единая БД (Neon PostgreSQL)** | pgvector + pg_trgm. Отказ от SurrealDB. Одна СУБД на всё | ADR-004 |
| P12 | **Единое сообщество, две юрисдикции** | Сообщество — одно. Юридические границы — только по закону (152-ФЗ, санкции) | ADR-007 |
| P-SLF | **AI Self-Learning Flywheel** | Артефакты участников обучают ИИ-агентов → агенты улучшают обучение → маховик. Множители по квалификации, epistemic gates | ADR-015 |

#### 3.1.3. Ссылки на 1-й уровень (FPF)

| Принцип | Источник |
|---|---|
| Role ≠ Executor | FPF A.7, операционализация: DP.D.033 |

### 3.2. Целевая архитектура v2 (диаграмма)

```
╔══════════════════════════════════════════════════════════════════════════╗
║                        Layer 3: ИНТЕРФЕЙСЫ                              ║
║                     (thin clients, UI-agnostic)                         ║
╠══════════════════════════════════════════════════════════════════════════╣
║                                                                          ║
║  ┌──────────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌────────┐║
║  │  ★ Web App   │  │ TG Bot   │  │ Claude   │  │ Discord  │  │ ChatGPT│║
║  │  (Next.js)   │  │ @aist_me │  │ Code CLI │  │ (Teams)  │  │  Apps  │║
║  │  Canonical   │  │ Fast ch. │  │ T4/T5    │  │ SC-8     │  │ Agents │║
║  └──────┬───────┘  └────┬─────┘  └────┬─────┘  └────┬─────┘  └───┬────┘║
║         │               │             │             │             │      ║
╠═════════╪═══════════════╪═════════════╪═════════════╪═════════════╪══════╣
║         └───────────────┴─────────────┴─────────────┴─────────────┘      ║
║                                    │                                     ║
║                          ┌─────────▼─────────┐                           ║
║                          │    API Gateway     │                           ║
║                          │  (ORY SSO + RBAC)  │                           ║
║                          └─────────┬─────────┘                           ║
║                                    │                                     ║
╠════════════════════════════════════╪══════════════════════════════════════╣
║                        Layer 2: ОБРАБОТКА                                ║
║                                    │                                     ║
║  ┌─────────────────────────────────┼────────────────────────────────┐    ║
║  │              Zone A: ИИ-системы (stateless, LLM)                │    ║
║  │                                 │                                │    ║
║  │  ┌──────────┐ ┌──────────┐ ┌───┴──────┐ ┌──────────┐           │    ║
║  │  │Стратег R1│ │Экстрактор│ │Проводник │ │Консультант│          │    ║
║  │  │ Grade 3  │ │  R2 Gr3  │ │ R13 Gr2  │ │  R3 Gr2  │          │    ║
║  │  └──────────┘ └──────────┘ └──────────┘ └──────────┘           │    ║
║  │  ┌──────────┐ ┌──────────┐ ┌──────────┐                        │    ║
║  │  │Оценщик   │ │Наладчик  │ │Статистик │                        │    ║
║  │  │R12 Gr2   │ │ R11 Gr2  │ │ R10 Gr1  │                        │    ║
║  │  └──────────┘ └──────────┘ └──────────┘                        │    ║
║  └──────────────────────────────────────────────────────────────────┘    ║
║                                    │                                     ║
║  ┌─────────────────────────────────┼────────────────────────────────┐    ║
║  │         Zone B: Детерминированные (stateful, MCP, code)         │    ║
║  │                                 │                                │    ║
║  │  ── MCP Hub (ADR-018) ──                                       │    ║
║  │  ┌──────────────────────────────────────────────────────────┐  │    ║
║  │  │★ MCP Hub   Registry ◄──► Gateway ◄──► Provisioner       │  │    ║
║  │  │  (единый    Platform MCP │ User MCP │ Community MCP     │  │    ║
║  │  │   endpoint) ORY OAuth2   │ per-user │ marketplace       │  │    ║
║  │  └──────────────────────────────────────────────────────────┘  │    ║
║  │       │              │              │              │            │    ║
║  │  ── Данные и знания (Platform MCP) ──                          │    ║
║  │  ┌─────────────┐  ┌──────────────┐  ┌──────────────┐           │    ║
║  │  │★ Digital    │  │★ Knowledge   │  │  Guides MCP  │           │    ║
║  │  │  Twin MCP   │  │   MCP        │  │  (→ Neon PG) │           │    ║
║  │  │ (private)   │  │  (public)    │  │  (public)    │           │    ║
║  │  └─────────────┘  └──────────────┘  └──────────────┘           │    ║
║  │  ┌─────────────┐  ┌──────────────┐                              │    ║
║  │  │★ Epistemic  │  │★ Unified     │                              │    ║
║  │  │  Graph (ESG)│  │  Search      │                              │    ║
║  │  │ (Proof-of-I)│  │ (cross-sys)  │                              │    ║
║  │  └─────────────┘  └──────────────┘                              │    ║
║  │                                                                  │    ║
║  │  ── Экономика и идентичность ──                                 │    ║
║  │  ┌─────────────┐  ┌──────────────┐  ┌──────────────┐           │    ║
║  │  │★ Billing &  │  │★ Loyalty /   │  │  ORY         │           │    ║
║  │  │  Payments   │  │  Proof-of-   │  │  (Identity)  │           │    ║
║  │  │  (Stripe+TG)│  │  Impact      │  │  SSO         │           │    ║
║  │  └─────────────┘  └──────────────┘  └──────────────┘           │    ║
║  │  ┌─────────────┐  ┌──────────────┐                              │    ║
║  │  │★ Certific-  │  │★ Revenue     │                              │    ║
║  │  │  ation Svc  │  │  Sharing     │                              │    ║
║  │  │ (diplomas)  │  │ (royalties)  │                              │    ║
║  │  └─────────────┘  └──────────────┘                              │    ║
║  │                                                                  │    ║
║  │  ── Обучение и контент ──                                       │    ║
║  │  ┌─────────────┐  ┌──────────────┐  ┌──────────────┐           │    ║
║  │  │  LMS        │  │★ Workbook /  │  │★ ДЗ-чекер   │           │    ║
║  │  │  Aisystant  │  │  Моделер     │  │  (AI HW     │           │    ║
║  │  │  (legacy→API│  │ (exercises)  │  │   Checker)  │           │    ║
║  │  └─────────────┘  └──────────────┘  └──────────────┘           │    ║
║  │  ┌─────────────┐  ┌──────────────┐  ┌──────────────┐           │    ║
║  │  │★ Personal   │  │★ Seminar /   │  │★ Mentor      │           │    ║
║  │  │  Guide Gen  │  │  Webinar Svc │  │  Panel /     │           │    ║
║  │  │  (WP-58)    │  │ (live events)│  │  Streams Mgmt│           │    ║
║  │  └─────────────┘  └──────────────┘  └──────────────┘           │    ║
║  │                                                                  │    ║
║  │  ── Оркестрация и взаимодействие ──                             │    ║
║  │  ┌─────────────┐  ┌──────────────┐  ┌──────────────┐           │    ║
║  │  │★ CRM /      │  │★ Activity    │  │★ Event Bus   │           │    ║
║  │  │  Onboarding │  │   Hub        │  │  (Orchestr.) │           │    ║
║  │  └─────────────┘  └──────────────┘  └──────────────┘           │    ║
║  │  ┌─────────────┐  ┌──────────────┐  ┌──────────────┐           │    ║
║  │  │★ Notific-   │  │★ Team        │  │  Composer    │           │    ║
║  │  │  ation Svc  │  │  Service     │  │  MCP (FSM)   │           │    ║
║  │  │(push/email) │  │  (SC-8)      │  │              │           │    ║
║  │  └─────────────┘  └──────────────┘  └──────────────┘           │    ║
║  └──────────────────────────────────────────────────────────────────┘    ║
║                                    │                                     ║
╠════════════════════════════════════╪══════════════════════════════════════╣
║                   Layer 1: ДАННЫЕ + ИНФРАСТРУКТУРА                       ║
║                                    │                                     ║
║  ┌──────────────┐  ┌──────────────┐│ ┌──────────────┐  ┌──────────────┐ ║
║  │★ Neon PG     │  │  GitHub      ││ │  Vercel /    │  │  Cloudflare  │ ║
║  │ (pgvector,   │  │  Repos (25+) ││ │  Railway     │  │  Workers     │ ║
║  │  RLS, Event  │  │              ││ │  → K8s       │  │  (MCP edge)  │ ║
║  │  Store)      │  │              ││ │              │  │              │ ║
║  └──────────────┘  └──────────────┘│ └──────────────┘  └──────────────┘ ║
║  ┌──────────────┐  ┌──────────────┐│ ┌──────────────┐                   ║
║  │  ORY Network │  │  Redis /     ││ │  Monitoring  │                   ║
║  │  (hosted)    │  │  CF KV       ││ │  (Grafana)   │                   ║
║  │              │  │  (cache)     ││ │              │                   ║
║  └──────────────┘  └──────────────┘│ └──────────────┘                   ║
╚══════════════════════════════════════════════════════════════════════════╝

★ = новый или существенно переработанный компонент
```

### 3.3. Ключевые изменения As-is → To-be

| # | Изменение | Обоснование |
|---|-----------|-------------|
| 1 | **Web App (Next.js)** как каноническая поверхность | ADR-001: международная аудитория, rich UI, PWA, «банковское приложение для развития» |
| 2 | **API Gateway + ORY SSO** | Единая точка входа для всех интерфейсов. SSO через ORY для Web App, бот, CLI |
| 3 | **SurrealDB → Neon** | Принцип P9: единая СУБД. Guides-mcp и docs publishing мигрируют на Neon |
| 4 | **Billing & Payments** | Новый сервис: Stripe (web) + TG Stars (бот) + LMS programs. Feature gating, подписки |
| 5 | **Loyalty / Proof-of-Impact** | Система баллов: начисление за активные действия (множитель по квалификации T2 x1, T3 x1.5, T4 x2), оплата программных продуктов баллами (1 балл ≈ $1) |
| 6 | **CRM / Onboarding** | Персональная воронка: SC-1 (онбординг) + конверсия T1→T5 |
| 7 | **Activity Hub** (расширен 29 мар, → **WP-109**) | **Платформенный журнал событий.** Фиксирует ВСЕ события на платформе (бот, LMS, клуб, Billing, CRM, ORY, Web App). pg_notify — внутренний механизм уведомления подписчиков. Source для ЦД, баллов, дашбордов. Реализация: WP-109 Ф5+Ф6 |
| ~~8~~ | ~~**Event Bus / Orchestration**~~ | ~~Поглощён Activity Hub (решение 29 мар). pg_notify = транспорт внутри Activity Hub~~ |
| 9 | **Team Service** (SC-8) | Формирование команд: подбор по ЦД, каналы (Discord), ИИ-системы как участники |
| 10 | **Discord** как дополнительная поверхность | SC-8: реал-тайм командная работа, голосовые чаты |
| 11 | **LMS Aisystant → API** | Legacy LMS обёртывается API-слоем для интеграции с Web App (не переписывается целиком) |
| 12 | **Railway → Kubernetes** | Долгосрочная миграция. Сначала Web App на Vercel, бот на Railway, потом всё в K8s |
| 13 | **Monitoring (Grafana Cloud)** | Решение архитектора 19 фев. Centralized observability |
| 14 | **RLS в Neon** | Предпочтительный вариант изоляции данных. Одна БД, фильтрация по user_id |
| 15 | **Notification Service** | Push/email/in-app. Критичен для UX «банковского приложения» — ежедневная привычка |
| 16 | **ДЗ-чекер → сервис** | Выделить из LMS в отдельный ИИ-сервис, доступный через Web App и бот |
| 17 | **Рабочая тетрадь / Моделер → Web App** | Из LMS в Web App: интерактивные упражнения, кейсы, симуляции |
| 18 | **Эпистемический граф (ESG)** | Граф знаний/компетенций экосистемы. Зависимость для Proof-of-Impact, сертификации |
| 19 | **Unified Search** | Кросс-системный поиск: знания + руководства + ЦД + посты + кейсы |
| 20 | **Certification System** | Цифровые сертификаты, верификация, привязка к ЦД и ESG |
| 21 | **Revenue Sharing** | Автоматическое распределение: platform 30%, author 50%, instructor 15%, curator 5% |
| 22 | **Генератор персонального руководства** (WP-58) | ИИ-генерация маршрута из ЦД + целей + контекста |
| 23 | **MCP Hub** (ADR-018) | Единый endpoint для всех MCP. Registry + Gateway + Provisioner. Пользователи создают свои MCP для Pack/DS. Community MCP каталог. Поглощает Apps SDK (#29) |

### 3.4. Двух-юрисдикционная архитектура (P12)

> **Принцип:** Одно сообщество. Две юрисдикции. Раздельные сайты и железо — всё остальное общее.

Экосистема работает в двух правовых пространствах: **Россия** (152-ФЗ, санкционные ограничения, блокировки сервисов) и **мир** (GDPR, международные платёжные системы, свободный доступ к сервисам). При этом сообщество **едино**: участник из России и участник из Берлина видят друг друга, обмениваются знаниями, работают в одних командах, имеют общие баллы и репутацию.

#### 3.4.1. Что разделено (сайты + железо)

| Компонент | Россия | Мир | Причина |
|-----------|--------|-----|---------|
| **Web App (сайт)** | `ru.aisystant.com` (или аналог) — хостинг в РФ | `aisystant.com` — Vercel / EU | Блокировки, латентность, 152-ФЗ |
| **Серверы (железо)** | Hetzner Russia / VK Cloud / Selectel | Railway EU → K8s EU | 152-ФЗ, доступность из РФ |
| **Платежи** | YooKassa, Paybox, TG Stars | Stripe, PayPal, TG Stars | Stripe/PayPal не работают в РФ |
| **PII (персональные данные)** | Хранение ПД граждан РФ в РФ (152-ФЗ ст.18) | Neon EU Frankfurt (GDPR) | 152-ФЗ требует локализации |

#### 3.4.2. Что общее (единое, без разделений)

| Компонент | Реализация |
|-----------|-----------|
| **Сообщество** | Единый Discourse (systemsworld.club), единый Discord, единые команды (SC-8) |
| **Баллы и репутация** | Единая система баллов, один эпистемический статус |
| **Цифровой двойник** | Единая модель ЦД, общие проекции, общие данные прогресса |
| **Знания и руководства** | Единый Knowledge MCP, единые руководства (RU + EN), общий контент |
| **ИИ-системы** | Одни и те же ИИ-системы для всех участников |
| **Activity Hub** | Общий поток активности, общая статистика |
| **Учётная запись** | Единый ORY SSO, один аккаунт на весь мир |
| **Billing логика** | Единые подписки, тарифы, feature gating. Различаются только платёжные адаптеры |
| **AI Training** | Единый пул артефактов для обучения ИИ-агентов (P-SLF) |
| **Обучение** | Единые программы, единые ДЗ, единый ДЗ-чекер, единая сертификация |

#### 3.4.3. Архитектура: два фронтенда, единый бэкенд

```
                         ПОЛЬЗОВАТЕЛИ
                    RU Россия    World Мир
                        |           |
               +--------v--+  +-----v--------+
               |  Web App  |  |   Web App     |
               |  RU site  |  |  World site   |
               | (РФ хост) |  |  (Vercel EU)  |
               +--------+--+  +-----+---------+
                        |           |
                        +-----+-----+
                              |
               +--------------v--------------+
               |       API Gateway           |
               |     (ORY SSO + RBAC)        |
               |  Routing по юрисдикции      |
               +--------------+--------------+
                              |
    +-------------------------+-------------------------+
    |         ЕДИНЫЙ ЛОГИЧЕСКИЙ СЛОЙ (бэкенд)           |
    |                                                     |
    |  Community . Loyalty . DT . Knowledge . AI systems   |
    |  Activity Hub . CRM . Certification . Search        |
    |  Event Bus . Notification . Team Service            |
    +-------------------------+---------------------------+
    |  Billing Adapter: RU    |  Billing Adapter: World   |
    |  YooKassa, Paybox, TGS  |  Stripe, PayPal, TGS     |
    +-------------------------+---------------------------+
    |    DB + PII: RU node    |    DB + PII: EU node      |
    |  (Hetzner RU / VK Cloud)|  (Neon EU Frankfurt)      |
    +-------------------------+---------------------------+
```

**Ключевое:** Два фронтенда — один код (Next.js), разный деплой. API Gateway определяет юрисдикцию по аккаунту и маршрутизирует к нужному платёжному адаптеру и PII-хранилищу. Вся бизнес-логика, знания, баллы, сообщество — общие.

**Billing:** Единый Billing Service с адаптерами (Strategy pattern). Пользователь привязывается к юрисдикции по стране регистрации.

**Данные — две ноды, один логический слой:**
- **РФ-нода:** PostgreSQL в российском ЦОД — ПД граждан РФ (ФИО, email, телефон, адрес) + реплика операционных данных для низкой латентности
- **EU-нода:** Neon EU Frankfurt — ПД международных пользователей + мастер-данные (знания, контент, баллы)
- **Синхронизация:** Event Bus обеспечивает eventual consistency между нодами
- Нужна юридическая консультация по точному разграничению

**ORY (уточнено 29 мар):** Два инстанса ORY: self-hosted в РФ + ORY Network EU. **Репликация** (не федерация). Механизм синхронизации identity между инстансами — через Event Bus.

#### 3.4.4. Открытые вопросы P12

| # | Вопрос | Контекст |
|---|--------|----------|
| Q9 | 152-ФЗ: точный scope локализации ПД | Какие данные обязательно в РФ? Юр. консультация |
| Q10 | Единый тариф в разных валютах? | RUB / USD / EUR — конвертация или региональные цены? |
| Q11 | Sanction compliance | Автоматическая проверка санкционных списков? |
| Q12 | Выбор РФ-хостинга | Hetzner Russia vs VK Cloud vs Selectel |
| Q13 | Репликация данных RU↔EU | Eventual consistency vs strong? |
| Q14 | Два юрлица или одно? | Влияет на 152-ФЗ, договоры, оплату |

### 3.5. Разделение руководств и рабочей тетради

**Source:** `Описание платформы обучения 3.2.md` + `Структура репозитория руководств 0.9.md`

| Компонент | Что | Доступ | Где |
|-----------|-----|--------|-----|
| **Тексты руководств** | Чтение теории, объяснения | ОТКРЫТЫЙ (docs/, MCP) | VitePress, Knowledge MCP |
| **Рабочая тетрадь** | Кейсы, упражнения, моделирование | ПО ПОДПИСКЕ | LMS → Web App |
| **Интерактивный моделер** | Симуляции, визуализации концепций | ПО ПОДПИСКЕ | LMS → Web App |
| **ДЗ-чекер** | ИИ-проверка заданий | ПО ПОДПИСКЕ | Бот / Web App |

**Репозиторий руководств:**
- Иерархическая структура: P.G.S.SS.T (Программа.Руководство.Раздел.Подраздел.ТипОбъекта)
- Один подраздел = один файл (гранулярный MCP-доступ)
- YAML frontmatter, assets/ для картинок
- Импорт из API (scripts/import_md/run.sh + metadata repo)

### 3.6. Новые сервисы (S44–S63)

Из 43 текущих сервисов **все сохраняются**. Добавляются:

| # | Новый сервис | Роль | Триггер | Исполнитель |
|---|-------------|------|---------|-------------|
| S44 | Team Matching | Проводник (R13) | On-demand | Web App + AI |
| S45 | Billing Check | Billing Service | Event (subscription change) | Deterministic |
| S46 | Points Mint | Loyalty Service | Event (confirmed action) | Deterministic |
| S47 | Onboarding Flow | CRM | Event (new registration) | Web App |
| S48 | Guide Import | Publisher (R21) | Scheduled | Neon (replaces SurrealDB) |
| S49 | Activity Feed | Activity Hub | Event (any user action) | Deterministic |
| S50 | Team Channel Setup | Team Service | Event (team formed) | Discord API |
| S51 | Send Notification (Nudge Engine) | Notification Svc | Event (ЦД insight, deadline, streak) + все 5 проекций ЦД | Push/email/in-app. 6 типов: адаптация, обучение, вовлечённость, признание, возвращение, траектория (DP.ARCH.003 §17) |
| S52 | Check Homework | ДЗ-чекер | On-demand (student submit) | AI + Web App |
| S53 | Issue Certificate | Certification Svc | Event (course completed + ESG verified) | Deterministic |
| S54 | Generate Personal Guide | Guide Generator | On-demand (user request) | AI + ЦД + Knowledge |
| S55 | Cross-system Search | Unified Search | On-demand | Neon pgvector + FTS |
| S56 | ESG Update | Epistemic Graph | Event (new evidence) | Deterministic + AI |
| S57 | Revenue Split | Revenue Sharing | Event (payment received) | Deterministic |
| S58 | Manage Stream | Stream Mgmt (T0) | On-demand (admin) | Web App (admin panel) |
| S59 | Grade Homework | Mentor Panel (TM) | On-demand (mentor) | Web App (mentor panel) |
| S60 | CRM Dashboard | CRM/Бэк-офис (T0) | On-demand (bookkeeper) | Web App (back-office) |
| S61 | **AI Training Pipeline** | AI Training Svc | Event (artifact confirmed, epistemic status ≥ peer-reviewed) | AI + Deterministic |
| S62 | **Event Access** | Event Access Svc (#28) | Scheduled (за N мин до события) или on-demand (admin) | Deterministic (JWT generation) + Notification (S51) |
| S63 | **Cloud Strategist** | Стратег (R1) | ⏰ ежедн 04:00 (cron сервер) | Anthropic API → GitHub API → DS-strategy |
| S64 | **My Digital Twin View** | R16 Ученик / R14 Заказчик | 👤 On-demand (`/me`, `/myprofile`, дашборд) | Мультиканальный: Bot + VS Code + Web App |
| S65 | **Statement Export** | R16 Ученик / R14 Заказчик | 👤 On-demand (`/export`, кнопка) | Deterministic (JSON/CSV/PDF) |

> **S63 Cloud Strategist (L2 Platform):** Серверная генерация DayPlan/WeekPlan. Заменяет локальный launchd (L4 Personal) для пользователей без надёжного Mac-пробуждения. Платформа по cron собирает коммиты через GitHub API, генерирует план через Anthropic API, коммитит в DS-strategy пользователя. Требует OAuth scope `repo` от пользователя. Нулевой setup: включается кнопкой в Web App. **Фаза:** 1+ (после Web App + ORY SSO). **Зависимости:** Web App (1.1), ORY SSO (0.2), GitHub OAuth.

> **S64 My Digital Twin View (мультиканальный):** Пользователь видит ВСЕ свои данные из ЦД через единый API — как банковское приложение. Принцип: **один API, много поверхностей** (бот, VS Code, Web App, будущие). Каждая поверхность адаптирует представление: бот → компактное TG-сообщение, VS Code → status line + webview, Web App → rich дашборд с графиками. Данные: профиль, активность (streak, calendar, events_7d/30d), обучение (feed/marathon/QA), IWE (коммиты, РП, coding_time, KE), тренды (дельта vs прошлая неделя), интеграции. **Audit trail запросов:** каждое обращение записывается как событие `dt_view_requested` в `user_events` — пользователь может видеть историю своих «выписок» (когда смотрел, что показывалось, с какими метриками). Аналогия: банковская выписка с датой и суммой. **Tier-gating:** T1 = базовые метрики (streak, calendar), T2 = + обучение, T3 = + IWE, T4 = + тренды + экспорт + AI-комментарий. **GDPR-aligned:** `/export` (S65) — полный экспорт своих данных (JSON/CSV/PDF). **Фаза:** 1.1+ (после Web App + ЦД). **Зависимости:** ЦД Event Store (0.6), ORY SSO (0.2).

### 3.7. Самообучение ИИ-агентов (P-SLF, ADR-015)

> **Архитектурный механизм AI Self-Learning Flywheel.** Платформа самоулучшается: артефакты учащихся обучают ИИ, а обученный ИИ улучшает качество обучения.

```
УЧАСТНИК                       ПЛАТФОРМА                        ИИ-АГЕНТЫ
────────                       ─────────                        ──────────
Учится, создаёт артефакт ─────► ЦД фиксирует прогресс
                               Наставник / peer-review
                               проверяет артефакт
                                        │
                               Epistemic status
                               подтверждён (≥ peer-reviewed)
                                        │
                               ┌────────▼────────┐
                               │  AI Training    │
                               │  Pipeline (S61) │
                               │  weight = role  │
                               │  × quality      │
                               └────────┬────────┘
                                        │
                                        ├────────────────────► ДЗ-чекер улучшает рубрики
                                        ├────────────────────► Консультант расширяет базу
                                        ├────────────────────► Проводник точнее маршруты
                                        └────────────────────► Оценщик калибрует критерии
                                        │
                               Улучшенные ИИ-агенты
                               лучше помогают учиться ──────► Участник учится эффективнее
                                                              создаёт ещё лучшие артефакты
                                                                        ↺ (маховик)
```

**Механизм:**

| Этап | Что происходит | Компонент |
|------|---------------|-----------|
| 1. Создание | Участник создаёт артефакт (ДЗ, кейс, проект, модель) | Рабочая тетрадь / IWE |
| 2. Валидация | Наставник / peer-review подтверждает качество | Панель наставника / ESG |
| 3. Классификация | Система присваивает epistemic status и вес автора | ESG + ЦД |
| 4. Обучение | Артефакт включается в тренировочный набор ИИ-агентов | AI Training Pipeline (S61) |
| 5. Улучшение | ИИ-агенты обновляют свои модели / промпты / рубрики | ИИ-системы (Zone A) |
| 6. Обратная связь | Улучшенные агенты дают более качественную помощь | Все поверхности |

**Множители веса артефакта в обучении:**

| Ролевая ступень автора | Множитель | Обоснование |
|------------------------|-----------|-------------|
| Ученик | ×1.0 | Базовые артефакты |
| Интеллектуал | ×1.5 | Структурированные артефакты |
| Профессионал | ×2.0 | Практические артефакты |
| Исследователь | ×3.0 | Фронтирные артефакты |
| Просветитель | ×4.0 | Методологические артефакты |

**Epistemic gates (что попадает в обучение):**

| Epistemic status | Попадает? | Пример |
|-----------------|-----------|--------|
| Предположение | Нет | Черновик ДЗ |
| Проверено наставником | Да (weight ×0.5) | Принятое ДЗ |
| Peer-reviewed | Да (weight ×1.0) | Кейс после рецензии |
| В production | Да (weight ×1.5) | Работающий проект |

**Реализация (фазы roadmap):**
- **Фаза 2:** Сбор и классификация артефактов (ESG, epistemic status). Ручная курация тренировочных наборов
- **Фаза 3:** Автоматический AI Training Pipeline (S61). Fine-tuning / RAG на артефактах. A/B-тестирование улучшенных агентов

**Безопасность:** PII удаляется из артефактов перед обучением. Участник может opt-out (артефакты не используются). Consent при регистрации (GDPR Art. 6).

### 3.8. MCP Hub — центр MCP-серверов (ADR-018)

> **АрхГейт 8.6/10 — пройден.** MCP Hub = централизованный реестр и runtime-прокси для всех MCP-серверов экосистемы. Заменяет planned «Apps SDK & Маркетплейс» (#29) — MCP и есть платформенный SDK.

#### 3.8.1. Проблема

Текущее состояние: 4 платформенных MCP-сервера (Knowledge, Guides, Digital Twin, Composer) настраиваются вручную в `claude_desktop_config.json` каждого клиента. Проблемы:

1. **Онбординг:** пользователю нужно знать адреса серверов, вручную прописывать конфигурацию
2. **Контроль доступа:** нет единой точки авторизации — каждый MCP реализует свою (или никакую)
3. **Расширяемость:** пользователь не может создать свой MCP для своих Pack/DS и опубликовать для других
4. **Клиент-агностичность:** конфигурация привязана к конкретному клиенту (Claude Desktop, Cursor, etc.)

#### 3.8.2. Решение: MCP Hub

MCP Hub — подсистема Layer 2 (Zone B), выполняющая три функции:

```
MCP Hub = Registry + Gateway + Provisioner

┌──────────────────────────────────────────────────────────────────┐
│                          MCP Hub                                  │
│                                                                    │
│  ┌─────────────┐  ┌─────────────────┐  ┌──────────────────────┐  │
│  │  Registry    │  │  Gateway        │  │  Provisioner         │  │
│  │  (каталог)   │  │  (прокси +      │  │  (создание user      │  │
│  │             │  │   авторизация)  │  │   MCP из шаблона)    │  │
│  └─────────────┘  └─────────────────┘  └──────────────────────┘  │
│         │                 │                       │                │
│  ┌──────▼──────┐   ┌──────▼──────┐        ┌──────▼──────┐        │
│  │ Platform    │   │ User MCP    │        │ Template    │        │
│  │ MCP (4+)    │   │ (per-user)  │        │ Gallery     │        │
│  └─────────────┘   └─────────────┘        └─────────────┘        │
└──────────────────────────────────────────────────────────────────┘
```

**Три типа MCP-серверов:**

| Тип | Владелец | Доступ | Примеры |
|-----|----------|--------|---------|
| **Platform MCP** | Платформа (L2) | По тиру (T1+ или публичный) | Knowledge, Guides, Digital Twin, Composer |
| **User MCP** | Пользователь (L4) | Только владелец + явный share | MCP для личного Pack, MCP для личного DS |
| **Community MCP** | Автор (L4, опубликовано) | По тиру + установка | MCP для предметной области (Pack-X), утилиты |

#### 3.8.3. Компоненты MCP Hub

**A. Registry (каталог)**

Neon DB, таблица `operations.mcp_registry`:

| Поле | Тип | Описание |
|------|-----|----------|
| `id` | UUID | PK |
| `owner_id` | UUID (FK → users) | NULL = платформенный |
| `name` | VARCHAR | Уникальное имя (namespace/name) |
| `type` | ENUM | platform / user / community |
| `endpoint` | VARCHAR | URL MCP-сервера |
| `transport` | ENUM | sse / streamable-http / stdio |
| `schema_snapshot` | JSONB | Кешированная MCP schema (tools, resources, prompts) |
| `access_tier` | VARCHAR | Минимальный тир для доступа (NULL = публичный) |
| `status` | ENUM | active / disabled / pending_review |
| `created_at` | TIMESTAMP | — |

RLS: пользователь видит `type='platform'` + `type='community' AND status='active'` + свои (`owner_id = current_user`).

**B. Gateway (прокси + авторизация)**

Cloudflare Worker, единый endpoint: `mcp-hub.aisystant.com`

Протокол:
1. Клиент (Claude Desktop, Cursor, etc.) подключается к MCP Hub по SSE/Streamable HTTP
2. MCP Hub проверяет ORY OAuth2 токен → определяет user_id, тир
3. Клиент вызывает `tools/list` → Hub агрегирует tools из всех доступных MCP пользователя
4. Клиент вызывает конкретный tool → Hub маршрутизирует к нужному MCP-серверу, передаёт auth context

```
Клиент (Claude/Cursor/etc.)
    │
    │ SSE / Streamable HTTP
    │ Authorization: Bearer <ory_token>
    │
    ▼
MCP Hub Gateway (CF Worker)
    │
    ├── Verify ORY token → user_id, tier
    ├── SELECT * FROM mcp_registry WHERE accessible(user_id, tier)
    ├── tools/list → aggregate from all MCPs
    │
    ├── tool call: "knowledge.search" → route to Knowledge MCP
    ├── tool call: "twin.read" → route to Digital Twin MCP (+ pass user_id)
    └── tool call: "my-pack.query" → route to User MCP (pack-personal)
```

**Преимущество:** пользователь прописывает ОДИН endpoint в своём клиенте. Все MCP доступны через Hub.

**C. Provisioner (создание пользовательского MCP)**

Пользователь (T3+) может создать MCP для своих Pack/DS.

**Принцип BYOB (Bring Your Own Backend):** данные пользователя хранятся на **его** ресурсах, не на платформенных. Платформа предоставляет код (шаблоны) и L2-знания, но не хостит пользовательские данные. Это снимает GDPR-ответственность с платформы и даёт пользователю полный контроль.

1. **Шаблон:** Платформа предоставляет шаблоны MCP-серверов:
   - `pack-mcp-template` — MCP для чтения Pack-репозитория (search, read entity, list methods)
   - `ds-mcp-template` — MCP для DS-репозитория (search docs, read processes)
   - `custom-mcp-template` — пустой MCP с boilerplate (auth, transport, tools scaffold)

2. **Knowledge Gateway (шаблон, L3):** Локальный прокси на машине пользователя. Объединяет L2 (платформенный, remote) и L4 (личный, local/remote) в единый MCP-endpoint. Пользователь подключает **один** MCP и не думает, откуда приходит ответ.

```
Клиент (Claude Code / Cursor / etc.)
    │  один MCP-endpoint
    ▼
Knowledge Gateway (локальный процесс)
    │
    ├── L2: Platform Knowledge MCP (remote, наш)
    │   ZP, FPF, SPF, платформенные Pack, courses, guides
    │   URL: knowledge-mcp.aisystant.workers.dev
    │
    └── L4: Personal Knowledge MCP (local или remote пользователя)
        Пользовательские Pack-*, DS-*, личные заметки
        Backend: Neon (его) / Supabase (его) / SQLite (локально)

    merge результатов по score → единый ответ
```

3. **Хранилище данных (BYOB):** Пользователь выбирает backend при setup:

| Backend | Бесплатный tier | Embeddings | Сложность setup | Для кого |
|---------|----------------|------------|-----------------|----------|
| **Neon** (PostgreSQL + pgvector) | 0.5 GB, 1 project | Через ingest (CF AI) | Низкая (уже обкатано) | Большинство |
| **Supabase** (PostgreSQL + pgvector) | 500 MB | Через ingest (CF AI) | Низкая | Альтернатива Neon |
| **SQLite** (локальный + sqlite-vss) | Безлимит | Через ingest (CF AI) | Нулевая (ничего не регистрировать) | Без доверия к облаку |

Общий интерфейс `KnowledgeBackend`:
- `search(query, embedding, limit) → SearchResult[]`
- `ingest(docs) → void`
- `listSources() → Source[]`

4. **Setup для пользователя** (в `setup.sh` шаблона IWE):

```bash
# Секция Knowledge Gateway в setup.sh
echo "=== Настройка Knowledge Gateway ==="
echo "Ваши Pack-данные хранятся у ВАС, не на нашем сервере."
echo "Выберите хранилище:"
echo "  1) Neon (рекомендуется, бесплатно до 0.5GB)"
echo "  2) Supabase (бесплатно до 500MB)"
echo "  3) Локальное (SQLite, без регистрации)"
read -p "Выбор [1]: " choice
# → записывает L4_BACKEND, L4_DATABASE_URL в ~/.iwe-env
# → запускает init-schema.ts (создание таблиц)
# → запускает ingest.ts (индексация Pack-папок)
```

5. **Синхронизация:** Sync-agent (R8 Синхронизатор) при Close или по cron вызывает `selective-reindex.sh` → ingest обновлённых Pack в L4 backend пользователя. L2 обновляется платформой независимо.

6. **Данные:** User MCP имеет доступ ТОЛЬКО к данным владельца:
   - Pack: индексируются из локальных файлов пользователя (не из GitHub)
   - DS: то же
   - ЦД: только свой digital twin (RLS по user_id в платформенном Neon)

7. **Деплой Knowledge Gateway:**
   - **Рекомендуемый:** Локальный процесс (`npx knowledge-gateway`), запускается через `.mcp.json` как stdio MCP
   - **Альтернативный (T4+):** Self-hosted CF Worker, регистрируется в Hub
   - **Managed deploy пользовательских данных — НЕ предоставляется.** Платформа не хранит чужие данные

8. **IWE-режим (SC-17):** Knowledge Gateway — ключевой enabler для режима «IWE как сервис». Участник T3+ получает Claude Code с загруженными Pack через BYOB-архитектуру. Это новая поверхность платформы наряду с ботом, LMS и Web App. См. [WP-74 SC-17](WP-74-platform-concept-of-use.md). Открытые вопросы:
   - **Модель оплаты Claude API:** (A) участник платит сам (Anthropic Pro $20/мес или API), (B) платформа проксирует (нужен API gateway + биллинг), (C) гибридная (L2-знания бесплатны, compute — на участнике)
   - **Тир:** T3 (персонализация) или T4 (созидание)? Связь с Q5 выше
   - **Онбординг:** `setup.sh` в `FMT-exocortex-template` уже покрывает клонирование и MCP — нужно добавить подключение к L2 (авторизация по Ory-токену)

9. **Distribution Pipeline (SC-18, 25 мар):** Обобщение SC-17 на уровень организации/партнёра. Aisystant = «Ubuntu для интеллекта» (DECK v0.6). Платформа поставляет **дистрибутив** — готовое окружение (Pack + шаблоны + MCP + AI-конфигурация), разворачиваемое поверх любого AI-провайдера.
   - **Distribution Pipeline:** `fork template` → `configure` → `provision MCP` → `connect AI provider` → `validate` → working IWE
   - **Knowledge Gate** (аналог Quality Gate в CI/CD): Pack-compliance, FPF-conformance, MCP-доступность, AI-провайдер
   - **AI Provider Abstraction:** vendor-agnostic интерфейс к LLM (Claude / GPT / Gemini / локальные). Кандидат ADR-019
   - **Data Portability:** Full Export API, открытые форматы (Git + Markdown + YAML), стандартизованный формат дистрибутива
   - **Infrastructure as External Service:** инфра-провиженинг = external (партнёрство с SRE-платформой), не in-house. Кандидат ADR-020
   - **Три контура:** L4 (деятель) / L2 (организация) / L3 (партнёр-fork)
   - Связь: ADR-018 (BYOB), WP-145 DECK v0.6, WP-73 context «design input дистрибутив». См. [WP-74 SC-18](WP-74-platform-concept-of-use.md)

#### 3.8.4. Связь с существующими подсистемами

| Подсистема | Связь с MCP Hub |
|-----------|----------------|
| **Knowledge MCP** (#5) | Регистрируется как Platform MCP. Доступ: публичный |
| **Guides MCP** (#6) | Регистрируется как Platform MCP. Доступ: публичный |
| **Digital Twin MCP** (#1) | Регистрируется как Platform MCP. Доступ: T1+ (приватные данные) |
| **Composer MCP** (#7) | Регистрируется как Platform MCP. Доступ: T1+ |
| **ORY SSO** (#10) | Hub Gateway использует ORY для аутентификации |
| **Apps SDK & Маркетплейс** (#29) | **Поглощается MCP Hub.** MCP = стандартный SDK. Маркетплейс = Community MCP каталог |
| **AI Training Pipeline** (#32) | Community MCP артефакты (schema, usage patterns) обогащают обучение |

#### 3.8.5. Сервисы MCP Hub

| # | Сервис | Триггер | Исполнитель |
|---|--------|---------|-------------|
| S64 | **MCP Discovery** | On-demand (client `tools/list`) | MCP Hub Gateway (CF Worker) |
| S65 | **MCP Route** | On-demand (client tool call) | MCP Hub Gateway (CF Worker) |
| S66 | **MCP Register** | On-demand (user creates MCP) | Web App → Neon |
| S67 | **MCP Provision** | Event (MCP registered, type=managed) | Provisioner → CF Workers API |
| S68 | **MCP Health Check** | Scheduled (every 5 min) | Hub → ping registered MCPs |
| S69 | **Knowledge Gateway Sync** | On Close / Scheduled (cron) | Local Gateway → ingest Pack changes → L4 backend пользователя |

#### 3.8.6. Тиры и MCP Hub

| Тир | Что доступно |
|-----|-------------|
| **T0** (бэк-офис) | Все Platform MCP + административные инструменты Hub |
| **T1** (старт) | Knowledge MCP, Guides MCP (публичные). Нет Digital Twin, нет Composer |
| **T2** (изучение) | + Digital Twin MCP (read-only), Composer MCP |
| **T3** (персонализация) | + Digital Twin MCP (read/write), + создание User MCP (до 3) |
| **T4** (созидание) | + создание User MCP (до 10), + публикация Community MCP |
| **T5** (владелец) | Без ограничений |

#### 3.8.7. Принцип #6 — Platform-space vs User-space + BYOB

MCP Hub реализует принцип отчуждаемости + принцип BYOB (Bring Your Own Backend):

- **Platform-space (L2):** Knowledge, Guides, Digital Twin, Composer — платформа управляет, обновляет, гарантирует SLA. Данные на платформенном Neon
- **User-space (L4):** User MCP — пользователь создаёт, управляет, может удалить. **Данные хранятся на ресурсах пользователя** (его Neon/Supabase/SQLite). Платформа предоставляет только код (шаблоны) и L2-знания, но не хостит и не имеет доступа к пользовательским данным
- **Community-space:** опубликованные User MCP. Автор управляет, платформа модерирует (review перед публикацией)

**Следствия BYOB:**

| Аспект | Платформа отвечает | Пользователь отвечает |
|--------|-------------------|----------------------|
| L2 данные (платформенные Pack, FPF, SPF) | Хранение, индексация, SLA | — |
| L4 данные (пользовательские Pack, DS) | — | Хранение, бэкапы, оплата |
| Код (gateway, ingest, шаблоны MCP) | Разработка, обновления | Установка, запуск |
| Embeddings API | Предоставление (CF AI, бесплатно) | Использование |
| GDPR / PII | Только за платформенные данные | За свои данные сам |

#### 3.8.8. АрхГейт MCP Hub (v2 — с BYOB)

| # | Характеристика | Оценка | Обоснование |
|---|---|---|---|
| Э | Эволюционируемость | **9** | Registry pattern + Backend interface: новый backend = реализовать interface. Новый Pack = строка в sources.json |
| М | Масштабируемость | **10** | BYOB: каждый пользователь на своём хранилище — горизонтальный масштаб бесплатно. L2 на CF Workers (scale-to-zero) |
| О | Обучаемость | **8** | setup.sh делает почти всё, но нужно завести Neon/Supabase аккаунт. SQLite-вариант = нулевой порог. Gateway прозрачен — один tool `search` |
| Г | Генеративность | **9** | Экосистема MCP: платформенные + пользовательские + community. Каждый Pack = новая ячейка знания, автоматически доступна |
| Ск | Скорость | **8** | L4 local (SQLite) <50ms. L4 remote (Neon) ~100ms. L2 ~300ms. Gateway параллелит → bottleneck = L2 |
| Со | Современность | **10** | BYOB + local-first + federated search = SOTA-паттерн (Context Engineering: Select + Compress). MCP = SOTA 2024-25 |
| Б | Безопасность | **10** | BYOB: данные пользователя НЕ на платформе. Нет PII. Нет GDPR-ответственности за L4. L2 — только публичное платформенное знание |
| **Итого** | | **9.1** | Порог ≥8 пройден. Улучшение с 8.6 → 9.1 за счёт BYOB (М +2, Со +1, Б +2) |

#### 3.8.9. ADR-018: MCP Hub как единая точка доступа к MCP (v2 — BYOB)

| Поле | Значение |
|------|----------|
| **Статус** | 🔶 Предлагается |
| **Контекст** | 4 MCP-сервера настраиваются вручную. Нет каталога, нет user MCP, нет единой авторизации. Хранение данных пользователей на платформе = GDPR-ответственность + trust barrier |
| **Решение** | MCP Hub: Registry (Neon) + Gateway (CF Worker) + Provisioner (шаблоны). **BYOB для L4**: данные пользователя на его ресурсах (Neon/Supabase/SQLite). Knowledge Gateway (локальный) объединяет L2 (платформа, remote) + L4 (личный, BYOB) в единый endpoint. Пользователь не знает, какой MCP отвечает |
| **Следствия** | Один endpoint для всех клиентов. Пользователи создают свои MCP. Apps SDK (#29) поглощается. Платформа НЕ хранит пользовательские данные |
| **Альтернативы** | (1) Оставить как есть (ручная конфигурация) — не масштабируется. (2) Multi-tenant Neon (все данные на платформе) — ЭМОГССБ 8.7, но Б=7 (GDPR). (3) Только Registry без Gateway — пользователю нужно прописывать endpoints |
| **Оценка** | ЭМОГССБ 9.1/10 (v2, улучшение с 8.6 за счёт BYOB) |

</details>
<details>
<summary><b>4. Анализ</b></summary>

### 4.1. Gap-анализ (31 подсистема)

#### 4.1.1. Подсистемы: As-is → To-be

| # | Подсистема | As-is | To-be | Gap | Приоритет |
|---|-----------|-------|-------|-----|-----------|
| 1 | **Web App** | ❌ Не существует | Next.js + Vercel/Railway, каноническая поверхность | 🔴 Создать с нуля | **P0** |
| 2 | **ORY SSO** | 🟡 Частично (digital-twin-mcp PKCE, LMS legacy) | Единый SSO для всех поверхностей + RBAC | 🟡 Интеграция Web App + миграция LMS | **P0** |
| 3 | **Цифровой двойник** | 🟡 MCP работает, Event Sourcing спроектирован, схемы в Neon | Полный 3-слойный ЦД: Events → State → Views, 5 проекций | 🟡 Реализовать Event Store + проекции | **P1** |
| 4 | **Billing + Revenue Sharing** | 🟡 TG Stars в боте, LMS на system-school.ru | Stripe + YooKassa + TG Stars (SOTA). Revenue Sharing движок. Unit economics. Feature gating | 🟡+🔴 SOTA-интеграция + уникальный Revenue Sharing | **P1** |
| 5 | **Knowledge MCP** | ✅ Работает (5400+ docs, 9 sources) | Добавить индексацию новых sources | 🟢 Минимальный | P3 |
| 6 | **Guides MCP** | 🟡 Работает, но на SurrealDB | Миграция на Neon PostgreSQL | 🟡 Миграция БД | **P1** |
| 7 | **Composer MCP (FSM)** | ✅ Работает | Без существенных изменений | 🟢 Минимальный | P3 |
| 8 | **TG Bot** | ✅ Работает (prod + pilot) | Deeplink на Web App | 🟢 Минимальный | P2 |
| 9 | **Claude Code IWE** | ✅ Работает (T4-T5) | Без изменений | 🟢 Минимальный | P3 |
| 10 | **LMS Aisystant** | ✅ Java 8, Vaadin 8, legacy | API-слой. Рабочая тетрадь → Web App | 🟡 API, интеграция с ORY | **P2** |
| 11 | **Discourse (Клуб)** | ✅ Работает | SSO интеграция с Web App | 🟢 SSO | P2 |
| 12 | **CRM / Бэк-офис (T0)** | ❌ Не существует | Customer journey, воронка, панели | 🔴 Создать (уникальная логика) | **P2** |
| 13 | **Loyalty / Proof-of-Impact** | ❌ Не существует | Система баллов + программа лояльности | 🔴 Создать | **P2** |
| 14 | **Activity Hub** | ❌ Не существует | Агрегатор событий | 🔴 Создать | **P2** |
| 15 | **Event Bus** | ❌ Не существует | Async event-driven интеграция | 🔴 Создать | **P1** |
| 16 | **Team Service** | ❌ Не существует | Формирование команд, каналы, ИИ-системы | 🔴 Создать | **P3** |
| 17 | **Реал-тайм каналы** | 🟡 Telegram уже используется | TG-группы или Web App чат | 🟡 Интеграция | **P3** |
| 18 | **ИИ-системы (11+ ролей)** | ✅ 7 систем + ассистенты | API для Web App, новые роли | 🟡 API, оркестрация | P2 |
| 19 | **Docs (VitePress)** | ✅ Работает, SurrealDB для publish | Миграция на Neon, SSO | 🟡 Миграция, SSO | **P1** |
| 20 | **Monitoring** | 🟡 Минимальный (error_logs) | Grafana Cloud | 🟡 Настроить | P2 |
| 21 | **Notification Service (Nudge Engine)** | 🟡 MVP в боте (6 правил, T3+, статичные тексты) | Push/email/in-app, nudges из ЦД. 6 типов: адаптация, обучение, вовлечённость, признание, возвращение, траектория. 4 фазы эволюции: правила → контекст → адаптация → агент. Архитектура: DP.ARCH.003 §7.4, §17 | 🔴 Расширить до платформенного сервиса | **P1** |
| 22 | **ДЗ-чекер** | 🟡 В LMS + частично бот | Единый сервис: Web App + бот + LMS | 🟡 Вынести в сервис | **P1** |
| 23 | **Certification System** | 🟡 Частично в LMS | Цифровые сертификаты + ЦД + ESG | 🟡 Расширить | **P2** |
| 24 | **Рабочая тетрадь / Моделер** | 🟡 В LMS | Интерактивная тетрадь в Web App | 🟡 Перенести | **P1** |
| 25 | **Персональное руководство** | ❌ Не существует (WP-58) | ИИ-генерация на основе ЦД | 🔴 Создать | **P2** |
| 26 | **Эпистемический граф (ESG)** | ❌ Не существует | Граф знаний/компетенций | 🔴 Создать | **P2** |
| 27 | **Unified Search** | ❌ Не существует | Кросс-системный поиск | 🔴 Создать | **P2** |
| 28 | **Семинары / Вебинары** | 🟡 Внешние инструменты (Zoom + TG-группа как gatekeeper) | JWT-based доступ: платформа выдаёт персональные ссылки оплатившим (ADR-016). Event Access Service (S62). Jitsi/Zoom/LiveKit — заменяемый компонент | 🟡 Создать | **P3** |
| 29 | **~~Apps SDK & Маркетплейс~~** → **MCP Hub** (ADR-018) | ❌ Не существует | Registry + Gateway + Provisioner. Единый endpoint для всех MCP. User/Community MCP. Поглощает Apps SDK | 🔴 Создать | **P2** |
| 30 | **Revenue Sharing** | ❌ Не существует | Объединён с Billing (#4) | → #4 | **P1** |
| 31 | **Панель наставника / Потоки** | ❌ Не существует | Табель, ДЗ, аналитика; потоки SC-10 | 🔴 Создать | **P1** |
| 32 | **AI Training Pipeline** | ❌ Не существует | Сбор артефактов → классификация → обучение ИИ-агентов (P-SLF) | 🔴 Создать (уникальная логика) | **P2** |
| 33 | **Планировщик мероприятий (Calendar Service)** | ❌ Не существует | Административный инструмент: планирование потоков и семинаров, быстрое редактирование расписания, экспорт в Google Calendar / iCal, публикация анонсов в бот. _Пожелание — scope уточняется (оплата, регистрация, автопубликация на сайт)._ | 🔴 Создать | **Backlog** |

#### 4.1.2. Сводка Gap (P16: Integrate SOTA, don't reinvent)

| Тип gap | Кол-во | Подсистемы |
|---------|--------|-----------|
| 🔴 **Создать уникальное** | 10 | CRM/Бэк-офис, Loyalty/Proof-of-Impact, Activity Hub, Revenue Sharing (в Billing), Панель наставника/Потоки, Personal Guide Gen, ESG, Team Service, **AI Training Pipeline**, Calendar Service (Backlog) |
| 🔵 **Интегрировать SOTA** | 5 | Web App (Next.js), Event Bus (Outbox + pg_notify), Notification Svc (Novu/Courier), Unified Search (pgvector + FTS), MCP Hub (Registry + Gateway) |
| 🟡 **Доработать существующее** | 11 | ORY SSO, ЦД, Guides MCP, LMS API, Docs, ИИ-системы, Monitoring, ДЗ-чекер, Certification, Рабочая тетрадь, Семинары |
| 🟢 **Минимальный gap** | 7 | Knowledge MCP, Composer MCP, TG Bot, Claude Code, Discourse, SystemsSchool Bot, Реал-тайм каналы (TG) |

### 4.2. Архитектурная оценка (ЭМОГССБ + Стоимость)

> АрхГейт (DP.M.005): порог ≥8. Оценка общей архитектуры платформы.
> Характеристики: DP.ARCH.001 §4. Принципы: §7. Покрытие: §7.1. Обратная связь: §8.

| # | Характеристика | Оценка | Обоснование |
|---|---------------|--------|-------------|
| Э | **Эволюционируемость** | **9** | Модульность (30 подсистем → 57 сервисов), Event Bus для loose coupling, Strategy pattern для billing/PII. Двух-юрисдикционность через адаптеры, не fork. **Риск:** 30 подсистем — много зависимостей; без Event Bus первые фазы будут tight-coupled |
| М | **Масштабируемость** | **8** | При 10x: Neon autoscaling, CF Workers global edge, Vercel SSR, K8s в Фазе 3. Две ноды (RU + EU). **Риск:** Neon free tier ограничен; Event Bus на pg_notify ≤1000 msg/sec |
| О | **Обучаемость** | **8** | 3-слойная архитектура (IPO), понятные тиры, role-centric модель, ASCII-диаграммы. **Риск:** 30 подсистем, 57 сервисов — ~2 дня погружение |
| Г | **Генеративность** | **9** | Работает в шаблоне экзокортекса. Создаёт платформу. MCP Hub (Registry + User/Community MCP). Global Core + Local Edge. Двойник + ESG + баллы + AI self-learning — замкнутый цикл роста |
| Ск | **Скорость** | **7** | Web App <1s, бот <3s, MCP <500ms. **Риск:** двойной деплой увеличивает CI/CD; репликация RU↔EU добавляет latency |
| Со | **Современность** | **9** | Next.js 15, MCP (SOTA.002), Event Sourcing, Role-centric (SOTA.001), Coupling Model (SOTA.011). Vercel + CF Workers = cloud-native |
| Б | **Безопасность** | **8** | ORY (OAuth2 PKCE), RLS, Knowledge≠UserData. GDPR + 152-ФЗ design. **Риск:** PII в двух юрисдикциях; нет WAF; санкционный compliance не решён |
| **Ст** | **Стоимость владения** | **7** | **Текущая:** ~$10-30/мес. **Целевая:** ~$350-800/мес (Vercel, Neon scaled, K8s, ORY, Grafana, РФ-хостинг). **LLM API:** $100-500/мес. **Итого to-be:** $450-1300/мес. **Плюсы:** free tiers, единая БД, SOTA. **Риски:** LLM масштабируется линейно; двух-юрисдикционность удваивает хостинг |

**Средний балл (ЭМОГССБ): 8.3/10** — проходит АрхГейт (порог ≥8).

**С учётом Стоимости: 8.1/10** — по-прежнему проходит.

> **Стоимость — предложение для АрхГейта:** характеристика «Стоимость владения» (TCO) добавлена в оценку этого РП. Рекомендуется добавить в DP.ARCH.001 §4 как 8-ю характеристику.

**Обратная связь Характеристика → Принцип** (DP.M.005 §8):
- **Скорость = 7** (слабое покрытие) → рекомендация: принцип «Latency budget per layer» (2-й уровень)
- **Стоимость = 7** (нет покрытия) → рекомендация: принцип «Cost-aware scaling» / «Free-tier first» (2-й уровень)

**Ключевые риски (управляемые):**
1. **Скорость CI/CD** при двойном деплое → единый Docker image + разные env
2. **Event Bus масштабируемость** → pg_notify → NATS/RabbitMQ (Фаза 2-3)
3. **Репликация RU↔EU** → чёткий data classification (Q13)
4. **LLM-стоимость** → кеширование, routing на Haiku, rate limiting по тирам
5. **30 подсистем** → фазированный roadmap (P0→P3)

### 4.3. ADR: архитектурные решения

> ✅ принято, 🔶 предлагается (требует согласования)

| # | Решение | Статус | Обоснование | Score |
|---|---------|--------|-------------|-------|
| ADR-001 | **Multi-surface с Web-ядром (Vue/Nuxt.js)** | ✅ | Международная аудитория, PWA, rich UI. Vue подтверждён 29 мар | 69/80 |
| ADR-002 | **Event Sourcing для ЦД** | ✅ | Immutable events, 5 проекций, rollback | 8.7 |
| ADR-003 | **Role-centric архитектура** | ✅ | Роль ≠ исполнитель | 50/60 |
| ADR-004 | **Neon PostgreSQL единая СУБД** | ✅ | Отказ от SurrealDB, pgvector, RLS | — |
| ADR-005 | **Railway → Kubernetes** | ✅ | Решение архитектора 19 фев | — |
| ADR-006 | **ORY Network для SSO** | ✅ | PKCE flow, LMS + MCP | — |
| ADR-007 | **Двойной деплой RU + EU** | 🔶 | Два сайта, единый бэкенд | 8.3 |
| ADR-008 | **Billing через Strategy pattern** | 🔶 | YooKassa + Stripe, единая логика | — |
| ADR-009 | **Activity Hub = платформенный журнал событий** | ✅ | Activity Hub расширяется на ВСЕ события платформы. pg_notify — транспорт доставки внутри Neon. Нет отдельного Event Bus — Activity Hub его поглощает. Решение 29 мар | Q4 |
| ADR-010 | **RLS для изоляции данных** | ✅ | Админам — все строки определённых таблиц. Студенты — ограничение на уровне сервисов. Решение 29 мар | Q1 |
| ADR-011 | **TG-группы для команд** | 🔶 | Аудитория уже в Telegram (P16) | Q6 |
| ADR-012 | **LMS не трогаем, новая архитектура рядом** | ✅ | Для новых пользователей. Существующий LMS продолжает работать. Решение 29 мар | Q7 |
| ADR-013 | **«Банковское приложение» UX** | 🔶 | Привычка ежедневной проверки | WP-65 |
| ADR-014 | **Трёхосевая модель доступов + Позиции** | ✅ | Permission = Entitlement ∩ Role ∩ Scope. Zanzibar ReBAC | 8.7 |
| ADR-015 | **Образованные агенты: fine-tuning на ZP/FPF** | 🔶 | Платформа выпускает методологически грамотных агентов. Синтетические данные из ZP/FPF | — |
| ADR-016 | **JWT-based доступ к семинарам/вебинарам** | 🔶 | Платформа выдаёт персональные JWT-ссылки только оплатившим. TG-привязка — канал доставки, не гейткипер | 8.6 |
| ADR-017 | **LLM Multi-Provider Fallback (shared library)** | 🔶 | Единая библиотека `llm_client` с fallback-цепочкой Claude → OpenAI. Embeddings — без fallback (несовместимы). ЭМОГССБ 8.3 | 8.3 |
| ADR-018 | **MCP Hub — единая точка доступа к MCP** | 🔶 | Registry (Neon) + Gateway (CF Worker) + Provisioner. Три типа MCP: Platform, User, Community. Поглощает Apps SDK (#29). ЭМОГССБ 8.6 | 8.6 |

#### ADR-014: Трёхосевая модель доступов + Позиции (DP.D.034)

> **Source-of-truth:** [`DP.D.034-access-control-model.md`](../../../PACK-digital-platform/pack/digital-platform/DP.D.034-access-control-model.md)

**Три оси:** Entitlement (тир) × Role (роль) × Scope (область). `Permission = Entitlement ∩ Role ∩ Scope`.

**Позиция** — именованный бандл (Role + Tier + Scope), назначаемый одним действием. В коде: `preset`.

| Позиция | Role | Tier | Scope |
|---------|------|------|-------|
| Наставник потока | Наставник | TM2 | `cohort:X/group:Y` |
| Куратор программы | Администратор + Автор | T0 | `program:Z` |
| Стажёр-наставник | Наставник | TM1 | `cohort:X/group:Y` (read-only) |
| Амбассадор | Амбассадор + Участник | T2+ | `referrals:*` |

**Реализация:** ORY Kratos (identity) + JWT claims (tier + roles) + **ORY Keto** (scope, ReBAC Zanzibar, ✅ подтверждено 29 мар) + таблица `positions` / `user_positions`. План внедрения: тестирование на боте → потом на Aisystant.

#### ADR-015: Образованные агенты — fine-tuning на ZP/FPF

> **Статус:** 🔶 предлагается
> **Связь:** P-SLF (AI Self-Learning Flywheel, §3.1.2), §3.7, S61

**Контекст.** Anthropic, OpenAI и другие провайдеры создают минимальных агентов — системы с общим образованием уровня «выпускник школы». Они умеют рассуждать, но не владеют методологической грамотностью: смешивают систему и описание, роль и исполнителя, уровень и контур.

Платформа Aisystant уже обучает людей нулевым (ZP) и первым (FPF) принципам. Те же принципы можно использовать для fine-tuning моделей — создавая **методологически грамотных агентов**, которые «из коробки» владеют корректным мышлением.

**Решение.** Платформа выпускает не только образованных людей, но и образованных агентов:

| Уровень образования | Что получает агент | Механизм |
|---------------------|-------------------|----------|
| Минимальный агент (Anthropic/OpenAI) | Общие способности | Pre-training провайдера |
| Методологически грамотный агент (Aisystant) | ZP + FPF: различения, принципы корректности, онтологическая дисциплина | Fine-tuning на синтетических данных из ZP/FPF |
| Доменный агент (пользователь) | Знания предметной области (Pack) | Context engineering (MCP, промпты) |
| Рабочий агент (пользователь) | Конкретные задачи (DS) | Постановка задач (промпты) |

**Источники обучающих данных:**

1. **ZP (30 ячеек).** Каждая ячейка содержит формализованное различение. Из каждого — 15-20 вариаций (бытовые, технические, управленческие контексты). Итого: 450-600 примеров.
2. **FPF.** Принципы корректности, типология (A.7), онтологические паттерны. Ещё 300-500 примеров.
3. **Бот-тренажёр (@aist_me_bot).** Уже содержит вопросы и ответы с оценками (pass/fail) — готовый верифицированный датасет.

**Генерация обучающих данных с помощью ИИ.** Принципы формализованы — у каждого различения есть чёткая граница правильного и неправильного. Это позволяет генерировать синтетические обучающие примеры:

1. Подать принцип/различение из ZP или FPF на вход модели
2. Сгенерировать разнообразные ситуации нарушения и корректного применения
3. Верифицировать через бота-тренажёра (автоматическая проверка)
4. Собрать в обучающий датасет (JSONL)

**Конкурентное преимущество.** Ни одна другая образовательная платформа не имеет формализованных принципов мышления в виде, пригодном для fine-tuning. ZP и FPF — уникальный актив, который можно конвертировать в образованных агентов.

**Связь с P-SLF (AI Self-Learning Flywheel, §3.1.2):**
- P-SLF описывает маховик: артефакты учащихся обучают ИИ → ИИ улучшает обучение
- ADR-015 описывает **начальное состояние** агентов в этом маховике: они начинают не с нуля, а уже методологически грамотными
- P-SLF обогащает агентов доменными знаниями (артефакты). ADR-015 даёт им фундамент корректного мышления (ZP/FPF)

**Реализация (фазы roadmap):**
- **Фаза 2:** Генерация синтетического датасета из ZP (30 ячеек × 20 вариаций). Пилотный fine-tuning через OpenAI API (gpt-4o-mini). A/B-тестирование: fine-tuned модель vs модель с ZP в контексте
- **Фаза 3:** Расширение на FPF. Fine-tuning Claude (при доступности Anthropic fine-tuning API). Интеграция в AI Training Pipeline (S61)

**Безопасность:** обучающие данные — синтетические, на основе публичных принципов. PII отсутствует. Модель не содержит персональных данных учащихся.

#### ADR-016: JWT-based доступ к семинарам и вебинарам

> **Статус:** 🔶 предлагается
> **Связь:** Подсистема #28 (Семинары/Вебинары), ADR-014 (трёхосевая модель доступов), Billing Service (S45), Notification Service (S51)

**Контекст.** Текущий сценарий платных семинаров (100+ человек):

1. Регистрация → ссылка на Zoom отправляется в Telegram-группу
2. Прогрев — все зарегистрированные в группе
3. Перед платной частью — бот кикает неоплативших из TG-группы
4. Без TG-группы = без ссылки на Zoom

**Проблемы текущего подхода:**

| Проблема | Следствие |
|----------|-----------|
| Привязка TG-аккаунта — блокирующее требование | Пользователь без Telegram не может участвовать |
| Ссылка на Zoom видна всем в группе до отсечки | Может утечь (переслать, скопировать) |
| Бот кикает N человек через TG API | Rate limits при 100+ участниках |
| Telegram-группа = единственный gatekeeper | Не масштабируется на другие каналы (web, email) |

**Решение.** Платформа контролирует доступ напрямую через персональные JWT-ссылки:

```
1. Пользователь регистрируется на семинар (web или бот)
2. Платформа создаёт Scope: event:seminar-{id}
3. Оплата → Billing Check (S45) → присвоение: role:participant + scope:event:seminar-{id}
4. TG-канал (read-only) — прогрев для ВСЕХ зарегистрированных (ссылки на встречу нет)
5. За 10 мин до платной части:
   → Event Access Service (S62) генерирует персональные JWT-ссылки ТОЛЬКО оплатившим
   → Notification Service (S51) доставляет ссылку: TG-бот / email / личный кабинет
6. Видеоплатформа принимает ТОЛЬКО участников с валидным JWT
```

**Mapping на модель доступа (ADR-014, DP.D.034):**

```
Permission = Entitlement ∩ Role ∩ Scope

Entitlement: T2+ (оплатившие семинар)
Role:        participant
Scope:       event:seminar-{id}

→ Результат: может получить персональную JWT-ссылку на встречу
```

**Telegram-привязка — стимул, не барьер:**

| Без привязки TG | С привязкой TG |
|-----------------|----------------|
| JWT-ссылка на email или в личном кабинете | JWT-ссылка в TG-бот + push-уведомление |
| Прогрев — только web (страница события) | Прогрев — TG-канал + web |
| Работает, но UX хуже | Полный UX |

**Видеоплатформа — заменяемый компонент:**

| Платформа | Механизм JWT-доступа | Self-hosted | Примечание |
|-----------|---------------------|-------------|------------|
| **Zoom** | Registration API: `POST /meetings/{id}/registrants` → персональный `join_url` | Нет | Проверенный, платный |
| **Jitsi Meet** | JWT-токен в URL: `https://meet.example.com/room?jwt={token}` | Да | Полный контроль, бесплатный |
| **LiveKit** | Room token с claims (identity, room, permissions) | Да | Современный WebRTC, low-latency |

Рекомендация: **Jitsi Meet** для self-hosted (полный контроль, JWT из коробки) или **Zoom Registration API** если остаёмся на Zoom.

**Сервис S62 — Event Access Service:**

| Параметр | Значение |
|----------|----------|
| Триггер | Расписание (за N минут до события) или on-demand (администратор) |
| Вход | `event_id` + список `user_id` с `Permission = Entitlement ∩ Role ∩ Scope` |
| Выход | Персональные JWT-ссылки (по одной на участника) |
| Доставка | Через Notification Service (S51): TG-бот, email, in-app |
| Аудит | Event: `access_link_generated { user_id, event_id, channel, timestamp }` |

**ЭМОГССБ:**

| Характеристика | Оценка | Обоснование |
|----------------|--------|-------------|
| Эволюционируемость | **9** | Видеоплатформа заменяема (Zoom → Jitsi → LiveKit). Каналы доставки расширяемы через S51 |
| Масштабируемость | **9** | При 10x — просто не генерируем ссылку. Нет операции кикания на каждого неоплатившего |
| Обучаемость | **7** | Нужно настроить JWT + видеоплатформу. Но API простой (один сервис S62) |
| Генеративность | **9** | Паттерн «персональная ссылка по Permission» работает для любого контента: семинары, вебинары, записи, файлы |
| Скорость | **9** | Генерация JWT — миллисекунды. Доставка через S51 — секунды. Нет polling/кикания |
| Современность | **9** | JWT-based access, Zero Trust (не доверяем групповым ссылкам). Jitsi/LiveKit — SOTA |
| Безопасность | **8** | Персональный JWT нельзя переиспользовать (exp + sub). Нет утечки через группу. Аудит-лог |
| **Среднее** | **8.6** | Проходит АрхГейт (порог ≥8) |

**Обратная связь Характеристика → Принцип:**
- Обучаемость = 7 (слабое покрытие) → митигация: пошаговая документация + готовый Docker-образ Jitsi с JWT

**Безопасность (§8.5):**
- JWT с `exp` (время жизни = длительность события + 15 мин буфер)
- JWT с `sub` (user_id) — одноразовый, привязан к пользователю
- HTTPS only для доставки ссылок
- Аудит-лог: кто получил ссылку, через какой канал, когда вошёл

**Реализация (фазы roadmap):**
- **Фаза 3 (roadmap 3.6):** Event Access Service (S62), интеграция с Jitsi/Zoom, UI управления событиями
- **Временное решение (до Фазы 3):** Zoom Registration API через скрипт — платформа регистрирует оплативших через API, без ручной работы

#### ADR-017: LLM Multi-Provider Fallback (shared library)

> **Статус:** 🔶 предлагается
> **Связь:** P2 (слабая связанность), P4 (IPO-единообразие), P5 (Evolvability-first), P20 (Integrate SOTA), SOTA — LiteLLM/aisuite (2024-2025)

**Контекст.** Платформа зависит от Anthropic Claude API как единственного LLM-провайдера для генерации текста (бот: 10 точек интеграции, hw-checker, утилиты). Сервис Claude API испытывает периодические outage (~1 день/неделю по наблюдениям). При недоступности API — деградация ИИ-функций для конечных пользователей.

**Карта LLM-зависимостей платформы:**

| Компонент | Провайдер | Тип использования | Критичность |
|-----------|-----------|-------------------|-------------|
| Бот (10 точек) | Anthropic (Sonnet, Haiku) | Chat Completion + Tool Use | Высокая |
| guides-mcp | OpenAI (embeddings) | Vector embeddings | Высокая |
| translate.py | OpenAI (GPT-4o) | Chat Completion | Низкая |
| embeddings.py | OpenAI (embeddings) | Vector embeddings | Низкая |
| hw-checker | Anthropic (через n8n) | Chat Completion | Средняя |
| classify/report | Anthropic (опционально) | Chat Completion | Низкая |

**Детализация LLM-зависимостей бота (10 точек):**

| Функция бота | Модель | Требует LLM? | При outage без fallback |
|-------------|--------|-------------|------------------------|
| Генерация урока (марафон) | Sonnet | Да | Не работает |
| Генерация вопроса (марафон) | Sonnet | Да | Не работает |
| Генерация практического задания | Sonnet | Да | Не работает |
| Q&A консультант (tool_use) | Sonnet | Да | Не работает |
| Оценка ответов (тренировка) | Haiku | Да | Не работает |
| Триаж фидбека | Haiku | Да | Не работает |
| Персонализация ленты ("почему") | Haiku | Да | Не работает |
| Валидация рабочих продуктов | Sonnet | Да | Не работает |
| Autofix ошибок | Sonnet | Да | Не работает |
| /mydata объяснения | Haiku | Да | Не работает |
| Навигация, меню, настройки, SM | — | Нет | **Работает** |
| Выбор тем, расписание | — | Нет | **Работает** |

> **Вывод:** ~80% пользовательского опыта бота зависит от LLM. Без fallback outage Claude = деградация основных функций.

**Решение.** Shared library `llm_client` с per-component fallback. НЕ отдельный сервис (gateway), а подключаемая библиотека.

**Два типа LLM-использования — два подхода:**

| Тип | Пример | Fallback | Почему |
|-----|--------|----------|--------|
| **Chat Completion** | Генерация, оценка, Q&A, tool_use | Claude → OpenAI (GPT-4o) → degraded mode | Модели совместимы по capability |
| **Embeddings** | guides-mcp, embeddings.py | **Без fallback** | Эмбеддинги несовместимы между провайдерами (разная размерность, разное пространство). Переключение = полная переиндексация |

**Архитектура:**

```
llm_client/              ← shared library (Python-пакет)
├── base.py              ← интерфейс: ask(prompt, tools?) → response
├── adapters/
│   ├── anthropic.py     ← адаптер Claude (Sonnet, Haiku)
│   └── openai.py        ← адаптер OpenAI (GPT-4o, GPT-4o-mini)
├── router.py            ← fallback chain + retry (3x exponential backoff)
└── config.py            ← приоритеты провайдеров, таймауты

Потребители:
  Бот:        from llm_client import ask    ← единый интерфейс
  hw-checker: from llm_client import ask
  translate:  from llm_client import ask
```

**Fallback chain:**

```
1. Claude API (primary) — retry 3x (1s, 2s, 4s)
   ↓ 529/503/timeout после 3 retry
2. OpenAI API (secondary) — retry 3x
   ↓ fail
3. Degraded mode: "ИИ-консультант временно недоступен"
   (навигация работает: меню, настройки, выбор тем. Генерация контента — нет: уроки, вопросы, оценка, Q&A требуют LLM)
```

**Tool Use:** формат различается между провайдерами. Адаптер транслирует Anthropic tool_use ↔ OpenAI function_calling. Единый internal формат в `base.py`.

**ЭМОГССБ:**

| Характеристика | Оценка | Обоснование |
|----------------|--------|-------------|
| Эволюционируемость | **9** | +провайдер = +адаптер. Единый интерфейс стабилен |
| Масштабируемость | **9** | Распределение нагрузки между провайдерами при 10x |
| Обучаемость | **8** | Shared library, не gateway. Разработчик видит код, не конфигурирует сервис |
| Генеративность | **8** | Паттерн переиспользуется всеми компонентами платформы |
| Скорость | **9** | Primary path — без overhead. Fallback — +2-4 сек на timeout |
| Современность | **9** | LiteLLM, aisuite — стандартный паттерн 2024-2025 |
| Безопасность | **8** | +1 API key (OpenAI). Секреты вне git (P10). Server-to-server |
| **Среднее** | **8.8** | Проходит АрхГейт (порог ≥8) |

**Обратная связь Характеристика → Принцип:**
- Все характеристики ≥8. Слабых нет.

**Безопасность:** промпты отправляются двум провайдерам (server-to-server). PII пользователей минимален (только имя + контекст обучения). API keys — в env vars, не в git.

**Реализация:**
- **Фаза 1 (текущая):** retry с exponential backoff в существующем `claude_client.py`
- **Фаза 2:** shared library `llm_client` + адаптер OpenAI + интеграция в бот
- **Фаза 3:** подключение hw-checker и утилит

</details>
<details>
<summary><b>5. Открытые вопросы</b></summary>

### 5.1. Для архитектора

| # | Вопрос | Предпочтительный вариант |
|---|--------|--------------------------|
| Q1 | Изоляция данных в Neon | ✅ **RLS** (29 мар). Админам — все строки определённых таблиц, студенты — ограничение на уровне сервисов |
| Q2 | ORY за границей | ✅ **Self-hosted в РФ + репликация** (не федерация) (29 мар) |
| Q3 | Web App hosting | ✅ **Vue + раздельные хостинги РФ + мир** (29 мар) |
| Q4 | Event Bus | ✅ **Activity Hub = платформенный журнал** (29 мар). Отдельного Event Bus нет — Activity Hub расширен на все события. pg_notify — транспорт. TODO: типология событий по системам, retention |
| Q5 | Discourse SSO | DiscourseConnect + ORY (не обсуждался 29 мар) |
| Q6 | Каналы для команд (SC-8) | 🔶 открыт (29 мар) |
| Q7 | LMS замена vs обёртка | ✅ **Не трогаем LMS**, новая архитектура рядом для новых пользователей (29 мар) |
| Q8 | Кеш | ✅ **CF KV** для MCP. Vercel KV ❌ не нужен (29 мар) |
| Q13 | Репликация данных RU↔EU | 🔶 **2 базы + механизм синхронизации** (29 мар). TODO: перечень данных по нодам |

### 5.2. Для юридической консультации

| # | Вопрос |
|---|--------|
| Q9 | 152-ФЗ: точный scope локализации ПД граждан РФ |
| Q11 | Санкционный compliance: автоматическая проверка? |
| Q14 | Два юрлица (РФ + международное) или одно? |

### 5.3. Бизнес-решения

| # | Вопрос |
|---|--------|
| ~~Q10~~ | ~~Единый тариф в разных валютах или региональные цены?~~ **Решено:** двуслойная модель (подписка T1-T4 + события on-demand), см. [Тарифы и пакеты 2.1](../../../A.Systems-Builder/A2.Systems-Builder/A2.1.Meaning/2.1.1.%20Продуктовые%20предложения/Тарифы%20и%20пакеты%202.1.md) |
| Q12 | Выбор РФ-хостинга (Hetzner Russia vs VK Cloud vs Selectel) |

### 5.4. Дополнительные (из обсуждения 19 фев)

| # | Вопрос | Статус |
|---|--------|--------|
| B1-B8 | Детальные решения по тирам T1→T4 | Частично в WP-74 |
| C4 | Unit economics по тирам | Открыт |
| C6 | 25 репо → масштабируемость | Открыт |
| — | Платформа Андрея (Discourse) | Открыт |
| — | Конфигурация ИИ-систем (что может/не может) | Открыт |

</details>
<details>
<summary><b>6. План работы</b></summary>

### 6.1. Прогресс (что сделано)

#### Phase 0: Сбор знаний (26 фев)
- [x] Инвентаризация всех репо и систем (22 активных)
- [x] Сбор всех текстов и обсуждений о платформе
- [x] Карта as-is (текущая архитектура)
- [x] Сбор идей (Web App, ЦД, биллинг, лояльность)

#### Phase 1: Архитектурный план (26–27 фев)
- [x] To-be архитектура v2 (принципы, диаграмма, 22 ключевых изменения)
- [x] ЭМОГССБ: **8.3/10** (проходит АрхГейт); + Стоимость → **8.1/10**
- [x] Gap-анализ: **31 подсистема** (8 уникальных, 5 SOTA, 11 доработать, 7 мин)
- [x] Roadmap: **4 фазы** (март 2026 → 2027+)
- [x] ADR: **15 решений** (7 принятых, 8 предлагаемых)
- [x] ADR-015: Образованные агенты — fine-tuning на ZP/FPF (предложение)
- [x] ADR-014: Трёхосевая модель доступов + Позиции (ЭМОГССБ 8.7)
- [x] 63 сервиса (43 + 20 новых S44–S63)
- [x] ADR-016: JWT-based доступ к семинарам/вебинарам (предложение, ЭМОГССБ 8.6)
- [x] WP-74: 9 ролей, ролевая траектория (5 ступеней), 11 сценариев, тиры, UX, матрицы
- [x] Двух-юрисдикционная архитектура (P12)
- [x] Терминология платформы (§1.5, связь с WP-63)

#### Актуализация (W11, 11 мар 2026)
- [x] WP-63 завершён (10 мар): миграция ID `DP.AGENT → DP.ROLE` — §1.5 обновлён
- [x] P21 обновлён: «AI Self-Learning Flywheel» → «Экзоскелет, не протез» (DP.ARCH.001 §7). Flywheel перенесён в проектный принцип P-SLF (§3.1.2)
- [x] ADR-017: LLM Multi-Provider Fallback — shared library (предложение, ЭМОГССБ 8.3)
- [x] DP.D.035 (единая политика данных IWE) — ссылка добавлена в §2.8, в таблицу решений §2.10
- [ ] **Согласование с архитектором:** Q1-Q14 (ожидание)
- [ ] **Юридическая консультация:** Q9, Q11, Q14 (ожидание)

### 6.2. Roadmap

#### 6.2.1. Фаза 0: Фундамент (март–апрель 2026, ~8 недель)

**Цель:** ORY SSO + CRM/Billing MVP + Knowledge Gateway MVP + Event Bus skeleton

| # | Задача | Зависимости | Бюджет | Результат | Статус |
|---|--------|-------------|--------|-----------|--------|
| ~~0.3~~ | ~~**Guides MCP → Neon** миграция~~ | ~~—~~ | ~~5-8h~~ | ~~Отказ от SurrealDB~~ | ~~✅ done (WP-168, 25 мар)~~ |
| ~~0.4~~ | ~~**Docs publish → Neon** миграция~~ | ~~0.3~~ | ~~5-8h~~ | ~~Единая БД~~ | ~~✅ done~~ |
| 0.1 | **Web App scaffold** (Vue/Nuxt, ORY auth, i18n, раздельный деплой RU+EU) | — | 20-30h | Каркас с авторизацией | ⏳ |
| 0.2 | **ORY SSO интеграция** для Web App | 0.1 | 10-15h | Единый вход | ⏳ |
| 0.5 | **Activity Hub расширение** — платформенный журнал событий (pg_notify + Neon). Все системы пишут в `development.user_events`. Типология событий + retention policy | — | 8-12h | Единый event store для всей платформы | ⏳ |
| 0.6 | **ЦД Event Store** (WP-24 Phase 1) | 0.5 | 10-15h | user_events, первая проекция | ⏳ |
| 0.7 | **CRM + Billing MVP** (WP-183) — Directus + Billing Module в боте + TG Stars + Neon schemas crm/finance | ORY | 48h | Оплата звёздами → чат, Directus для sales | ⏳ |
| 0.8 | **Knowledge Gateway MVP** (WP-187) — L2 Platform endpoint (CF Worker) + BYOB спецификация + MVP sync | ORY | 15h | ≥1 T4-пользователь через remote MCP | ⏳ |

**Milestone:** SurrealDB убран ✅, ORY SSO, CRM для sales-команды, Knowledge Gateway L2 endpoint, Web App scaffold.

#### 6.2.2. Фаза 1: Core Product (май–июль 2026, ~12 недель)

**Цель:** Платный продукт с «банковским» UX

| # | Задача | Зависимости | Бюджет |
|---|--------|-------------|--------|
| 1.1 | **Web App: дашборд + ЦД** | Фаза 0 | 30-40h |
| 1.2 | **Web App: обучение** (руководства + тетрадь + ДЗ-чекер, [SC.117](../../../../PACK-digital-platform/pack/digital-platform/08-use-cases/DP.SC.117-async-homework-review.md)) | 1.1 | 30-40h |
| 1.3 | **Billing Phase 1** (WP-183) — YooKassa/Stripe адаптеры + idempotency + revenue sharing + Metabase | 0.7, ORY | 40h |
| 1.4 | **ЦД: проекции** (BKT, HLR) | Фаза 0.6 | 15-20h |
| 1.5 | **LMS ↔ новая архитектура: Event Bus bridge** — синхронизация событий между LMS и Neon через pg_notify. LMS не заменяется — новая платформа работает рядом для новых пользователей | 0.5, 1.2 | 15-20h |
| 1.6 | **CRM Phase 2** — identity linking, B2B лицензии, feature gating, at-risk | 0.7 | По мере роста |
| 1.7 | **Notification Service (Nudge Engine Фаза 2)** — все тиры T0–T4, 6 типов nudges, AI-генерация через Haiku, динамический cooldown. Архитектура: DP.ARCH.003 §17 | 1.1, Event Bus, ЦД | 15-20h |
| 1.8 | **ДЗ-чекер как сервис** | 1.5 | 10-15h |
| 1.9 | **Панель наставника / Потоки** (SC-10) | 1.2, 1.8 | 15-20h |
| 1.10 | **Cloud Strategist** (S63): серверная генерация DayPlan | 1.1, ORY, GitHub OAuth | 15-20h |

**Milestone:** Пользователь: регистрация → онбординг → обучение → ДЗ → оплата → прогресс → уведомления. Наставник: табель → ДЗ → обратная связь. Администратор: потоки → наставники.

#### 6.2.3. Фаза 2: Экосистема (август–декабрь 2026, ~20 недель)

**Цель:** Баллы + ESG + команды + сертификация + AI self-learning + Knowledge Gateway BYOB

| # | Задача | Зависимости | Бюджет |
|---|--------|-------------|--------|
| 2.1 | **ESG** (эпистемический граф) | ЦД, Knowledge MCP | 20-30h |
| 2.2 | **Loyalty / Proof-of-Impact** | Billing, Event Bus, ESG | 20-30h |
| 2.3 | **Activity Hub Phase 2** (WP-109) — подключение всех источников (LMS, клуб, Billing, CRM). Платформенный журнал уже запущен (Фаза 0.5) | Фаза 0.5, Billing | 15-20h |
| 2.4 | **Certification System** | ESG, ЦД | 15-20h |
| 2.5 | **Personal Guide Gen** (WP-149) | ЦД, Knowledge MCP, WP-175 | 15-20h |
| 2.6 | **Unified Search** | Knowledge, Guides, ЦД | 10-15h |
| 2.7 | **Team Service** (SC-8) | Activity Hub, ЦД | 20-30h |
| 2.8 | **Discord/Zulip интеграция** | Team Service | 15-20h |
| 2.9 | **Monitoring** (Grafana Cloud) | — | 8-12h |
| 2.10 | **ЦД: LLM-экстракция** (WP-24 Phase 3) | 1.4 | 15-20h |
| 2.11 | **Web App: сообщество** (Discourse SSO) | Фаза 1 | 10-15h |
| 2.12 | **ИИ-системы → Web App** (API) | Фаза 1 | 10-15h |
| 2.13 | **AI Training Pipeline** (P-SLF): сбор артефактов, epistemic gates, курация | ESG, ЦД | 15-20h |
| 2.14 | **Knowledge Gateway BYOB** (WP-187 Phase 2+) — полный BYOB pipeline, Knowledge Gate, Community MCP | 0.8, ORY SSO, Neon, CF Workers | 15-25h |

**Milestone:** Полная экосистема: обучение + баллы + ESG + сертификация + команды + AI self-learning + Knowledge Gateway BYOB + мониторинг.

#### 6.2.4. Фаза 3: Масштабирование (2027+)

| # | Задача |
|---|--------|
| 3.1 | Kubernetes миграция (Railway → K8s) |
| 3.2 | Международная локализация (EN → другие) |
| 3.3 | ~~Apps SDK / маркетплейс~~ → поглощён MCP Hub (Фаза 2, задача 2.14) |
| 3.4 | Revenue Sharing (автоматическое распределение) |
| ~~3.5~~ | ~~LMS декомпозиция (замена Vaadin 8)~~ — **отменено:** LMS не трогаем (решение 29 мар). Новая архитектура растёт рядом |
| 3.6 | Семинары / Вебинары: Event Access Service (S62), JWT-based доступ (ADR-016), Jitsi/Zoom интеграция, UI управления событиями |
| 3.7 | Mobile App (PWA / нативное) |
| 3.8 | Рабочая тетрадь v2: Моделер |

### 6.3. Timeline

```
2026      Mar    Apr    May    Jun    Jul    Aug    Sep    Oct    Nov    Dec    2027
          ├──────┤──────┤──────┤──────┤──────┤──────┤──────┤──────┤──────┤
          │ Фаза 0: Фундамент       │                                    │
          │ ✅ SurrealDB→Neon (мар)  │                                    │
          │ ORY SSO + Web App scaff  │                                    │
          │ CRM+Billing MVP (WP-183) │                                    │
          │ KG MVP (WP-187)          │                                    │
          │ Event Bus + ЦД Event St  │                                    │
          ├──────────────────────────┤                                    │
                                     │ Фаза 1: Core Product              │
                                     │ Дашборд + обучение + billing      │
                                     │ Billing Phase 1 (адаптеры+Metabase)│
                                     │ Notifications + ДЗ-чекер          │
                                     │ CRM Phase 2 + Панель наставника   │
                                     ├────────────────────┤              │
                                                          │ Фаза 2: Экосистема
                                                          │ ESG + баллы  │
                                                          │ Certification│
                                                          │ Teams+Search │
                                                          │ KG BYOB     │
                                                          │ Monitoring   │
                                                          ├──────────────┤
                                                                   2027 → Фаза 3
                                                                   K8s, Revenue Sharing,
                                                                   Семинары, Mobile
```

### 6.4. Следующие шаги

**Архитектурные решения (все приняты 29 мар ✅)**
~~1. Согласовать с архитектором: Блок А (Q1-Q7) — **done 29 мар**~~

**Открытые вопросы**
1. **Типология событий Event Bus** — входы/выходы по каждой системе, retention policy (TODO по А2)
2. **Перечень данных для репликации RU↔EU** — какие таблицы в каждой ноде (Б8)
3. **Встреча 2 с архитектором:** Knowledge Gateway (В1-В9)
4. **Юридическая консультация:** Q9 (152-ФЗ), Q11 (санкции), Q14 (юрлица) — влияет на Б8

**Реализация (Phase 0)**
5. **CRM Phase 0** (WP-183) → после Ory: Directus + Billing Module + TG Stars + Neon schemas
6. **Knowledge Gateway MVP** (WP-187) → после Ory: L2 Platform endpoint + BYOB спецификация
7. **Web App scaffold** (Vue/Nuxt, ORY auth, раздельный деплой RU+EU) — обновить WP-65
8. **Event Bus skeleton** (Faza 0.5) — начать после ADR-009 ✅

~~4. Специфицировать Billing → **done:** WP-183~~
~~5. Запланировать SurrealDB миграцию → **done:** WP-168 (25 мар)~~

### 6.5. Решения по монетизации и тирам (12 мар 2026)

> Детальный план действий → WP-85 (бот-задачи до/после Ory) и WP-7 (бот: техдолг).
> Архитектурные решения зафиксированы в DP.ARCH.002 (source-of-truth).

**Зафиксированные решения:**
- 4-осевая модель тиров: T0–T4 + TM/TA/TD (АрхГейт 8.9)
- Trial 30 дней от /start — все функции бота бесплатно (АрхГейт 8.6)
- TG Stars = только донаты, НЕ подписка
- Подписка БР на system-school.ru = единственный платёж
- T3/T4 = бесплатное подключение функций (ЦД, setup.sh)

</details>
<details>
<summary><b>7. Knowledge Gateway + BYOB (предложение)</b></summary>

> **Контекст:** ADR-018 (MCP Hub) описывает единую точку доступа к MCP-серверам. Это предложение уточняет модель данных для пользовательских MCP — как хранить знания пользователя и как объединять платформенные + пользовательские источники в один endpoint.

<details>
<summary><b>1. Проблема</b></summary>

Сейчас knowledge-mcp — монолит: 11 источников (4 платформенных Pack + SPF/FPF + DS + guides + личные) в одной Neon DB. Проблемы:

1. **Пользователь не может подключить свой Pack.** У каждого пользователя IWE будут собственные Pack-репо (их предметные области). Текущий knowledge-mcp — это наш сервер, пользователь не может добавить туда свои источники.

2. **Смешаны контуры.** Платформенные знания (ZP, FPF, SPF, PACK-digital-platform) и личные знания (DS-Knowledge-Index-Tseren, aist-bot-docs) — в одной базе. Нет изоляции.

3. **Масштаб.** При 100 пользователях × 1000 docs = 100K записей в одном Neon. Платформа платит за хранение чужих данных.

4. **Доверие.** Pack пользователя = конфиденциальное доменное знание (его бизнес). Хранение на нашем сервере = trust barrier.

5. **GDPR.** Если храним данные пользователей — мы data processor со всеми обязательствами.

</details>
<details>
<summary><b>2. Предлагаемое решение: BYOB (Bring Your Own Backend)</b></summary>

**Принцип:** платформа даёт код и L2-знания. Данные пользователя — на **его** ресурсах. Пользователь не думает, какой MCP спрашивать — один endpoint, за кулисами fan-out по источникам.

### 2.1. Два контура

| Контур | Что индексирует | Где живёт | Кто владеет |
|---|---|---|---|
| **L2 Platform** | ZP, FPF, SPF, платформенные Pack, courses, guides, FMT-docs | Remote (наш CF Worker + Neon) | Платформа |
| **L4 Personal** | Пользовательские Pack-*, DS-*, личные заметки | BYOB: его Neon / Supabase / SQLite | Пользователь |

### 2.2. Knowledge Gateway (локальный прокси)

```
Клиент (Claude Code / Cursor / бот)
    │  один MCP-endpoint (stdio или SSE)
    ▼
Knowledge Gateway (локальный процесс на машине пользователя)
    │
    ├── L2: Platform Knowledge MCP (remote, наш)
    │   URL: knowledge-mcp.aisystant.workers.dev
    │   Источники: ZP, FPF, SPF, PACK-DP, PACK-education, courses, guides
    │
    └── L4: Personal Knowledge MCP (BYOB)
        Backend: Neon (его) / Supabase (его) / SQLite (локально)
        Источники: PACK-my-domain-1, PACK-my-domain-2, DS-*

    Алгоритм:
    1. search(query) → параллельно в L2 и L4
    2. Merge результатов по score (cosine similarity)
    3. Единый ответ клиенту
```

### 2.3. Backend Interface

Общий контракт для любого L4-хранилища:

```typescript
interface KnowledgeBackend {
  search(query: string, embedding: number[], limit: number): Promise<SearchResult[]>
  ingest(docs: Document[]): Promise<void>
  listSources(): Promise<Source[]>
}

// Реализации:
class NeonBackend implements KnowledgeBackend { ... }
class SupabaseBackend implements KnowledgeBackend { ... }
class SqliteBackend implements KnowledgeBackend { ... }
```

### 2.4. Варианты хранилища для пользователя

| Backend | Бесплатный tier | Vector search | Setup |
|---------|----------------|---------------|-------|
| **Neon** (pgvector) | 0.5 GB | pgvector cosine | Низкая — обкатано |
| **Supabase** (pgvector) | 500 MB | pgvector cosine | Низкая |
| **SQLite** (sqlite-vss) | Безлимит | sqlite-vss cosine | Нулевая |

### 2.5. Setup для пользователя

В `setup.sh` шаблона IWE добавляется секция:

```bash
echo "=== Настройка Knowledge Gateway ==="
echo "Ваши Pack-данные хранятся у ВАС."
echo "Выберите хранилище:"
echo "  1) Neon (рекомендуется, бесплатно до 0.5GB)"
echo "  2) Supabase (бесплатно до 500MB)"
echo "  3) Локальное (SQLite, без регистрации)"
read -p "Выбор [1]: " choice
# → записывает L4_BACKEND, L4_DATABASE_URL в ~/.iwe-env
# → init-schema.ts (создание таблиц)
# → ingest.ts (первичная индексация Pack-папок)
```

### 2.6. Синхронизация

Sync-agent (R8 Синхронизатор) при Close или по cron:
- `selective-reindex.sh` → ingest обновлённых Pack в L4 backend пользователя
- L2 обновляется платформой независимо (weekly full + selective on commit)

</details>
<details>
<summary><b>3. Что меняется в текущей архитектуре</b></summary>

| Компонент | Сейчас | Станет |
|---|---|---|
| **knowledge-mcp** | Monolith: 11 sources, 1 user | Разделяется на: L2 (platform, remote) + L4 (personal, BYOB) |
| **Neon DB** | Без tenant_id, все данные вместе | Платформенный Neon = только L2. L4 = на ресурсах пользователя |
| **ingest.ts** | Прямой доступ к Neon | + Backend interface для разных хранилищ |
| **sources.json** | Один файл, 11 источников | L2 sources (платформенные, на сервере) + L4 sources (пользовательские, локальный конфиг) |
| **search** | Один SQL-запрос | Fan-out: L2 (remote HTTP) + L4 (local backend) → merge |
| **setup.sh** | Нет MCP-регистрации | + Выбор backend + init-schema + первичный ingest |
| **selective-reindex.sh** | Прямой ingest в Neon | + Ingest через Backend interface (может быть SQLite) |

</details>
<details>
<summary><b>4. Альтернативы (рассмотрены и отклонены)</b></summary>

| Вариант | ЭМОГССБ | Почему отклонён |
|---|---|---|
| **A. Оставить как есть** | — | Пользователь не может подключить свой Pack. Не масштабируется |
| **B. Multi-tenant Neon** (все данные на платформе, tenant_id) | 8.7 | Безопасность = 7 (GDPR, trust barrier). Платформа хранит чужие бизнес-данные |
| **C. Полностью локальный** (SQLite only, без remote L2) | 8.0 | Обучаемость = 6 (нужен Node.js, embeddings API). Нет платформенных знаний без интернета |
| **D. BYOB** (предлагаемый) | **9.1** | Лучший баланс: безопасность = 10 (данные не уходят), масштаб = 10 (каждый на своём), обучаемость = 8 (setup.sh) |

</details>
<details>
<summary><b>5. АрхГейт (ЭМОГССБ 9.1)</b></summary>

| Характеристика | Оценка | Обоснование |
|---|---|---|
| Э — Эволюционируемость | **9** | Backend interface: новый backend = реализовать interface. Новый Pack = строка в sources.json |
| М — Масштабируемость | **10** | BYOB: каждый пользователь на своём хранилище. L2 на CF Workers (scale-to-zero) |
| О — Обучаемость | **8** | setup.sh делает почти всё. SQLite = нулевой порог. Neon/Supabase = завести аккаунт |
| Г — Генеративность | **9** | Каждый Pack = новая ячейка знания, автоматически доступна через gateway |
| Ск — Скорость | **8** | L4 local (SQLite) <50ms. L4 remote (Neon) ~100ms. L2 ~300ms. Параллельный fan-out |
| Со — Современность | **10** | BYOB + local-first + federated search = SOTA. Context Engineering: Select + Compress |
| Б — Безопасность | **10** | Данные пользователя НЕ на платформе. Нет PII. Нет GDPR-ответственности за L4 |
| **Итого** | **9.1** | Порог ≥8 пройден |

</details>
<details>
<summary><b>6. Вопросы для обсуждения</b></summary>

### 6.1. Архитектурные

| # | Вопрос | Варианты | Моё предпочтение |
|---|---|---|---|
| Q1 | **Gateway как отдельный процесс или встроенный в personal-mcp?** | (A) Отдельный процесс — 3 артефакта. (B) Personal-mcp сам ходит в L2 — 2 артефакта | B — меньше движущихся частей |
| Q2 | **Embeddings для L4 — кто считает?** | (A) Cloudflare AI (бесплатно, remote). (B) Локальная модель (e.g. all-MiniLM). (C) OpenAI API (платно) | A — уже работает для L2, единая модель |
| Q3 | **Формат merge результатов L2+L4** | (A) Простой merge по score. (B) Weighted merge (L4 boost для персональных запросов). (C) Cascade: сначала L4, потом L2 если мало результатов | A для MVP, B позже |
| Q4 | **Как gateway подключается к клиенту?** | (A) stdio MCP (через .mcp.json). (B) SSE/HTTP (localhost:PORT). (C) Через MCP Hub (remote) | A — стандартный для Claude Code |

### 6.2. Продуктовые

| # | Вопрос |
|---|---|
| Q5 | С какого тира доступен Knowledge Gateway? T3 (персонализация) или T4 (созидание)? |
| Q6 | Нужен ли Web App UI для управления sources (добавить/удалить Pack)? Или только CLI/config? |
| Q7 | Community MCP: может ли пользователь A опубликовать свой Pack-MCP для пользователя B? Если да — через Hub или P2P? |

### 6.3. Миграционные

| # | Вопрос |
|---|---|
| Q8 | Мигрировать текущий knowledge-mcp сразу или поэтапно? (Фаза 1: выделить L2. Фаза 2: добавить L4 + gateway) |
| Q9 | DS-Knowledge-Index-Tseren и aist-bot-docs — переносить в L4 сейчас? Это breaking change для текущих сессий Claude Code |

</details>
<details>
<summary><b>7. Связанные документы</b></summary>

- **[WP-73 §3.8](WP-73-aisystant-platform-architecture.md)** — MCP Hub (ADR-018 v2, обновлён 17 мар)
- **[DP.D.036](../../../PACK-digital-platform/pack/digital-platform/01-domain-contract/DP.D.036-byob-knowledge-architecture.md)** — Различение BYOB vs Managed
- **[DP.D.031](../../../PACK-digital-platform/pack/digital-platform/01-domain-contract/DP.D.031-mcp-access-model.md)** — MCP Access Model
- **[DP.D.035](../../../PACK-digital-platform/pack/digital-platform/01-domain-contract/DP.D.035-data-policy.md)** — Единая политика данных IWE
- **[PACK-verification](../../../PACK-verification/)** — Домен верификации и приёмки (WP-122)

</details>

</details>

<details>
<summary><b>§3.9. Ролевое разделение агентов (верификация)</b></summary>

> Добавлено: 2026-03-17 (WP-122). Source-of-truth: PACK-verification (VR).

### Проблема

Один агент с одним контекстом не может проверить собственную работу — воспроизводит те же ошибки и слепые пятна (VR.FM.001).

### Решение: роли с разным контекстом

Разделение не по агентам, а по **ролям с разным контекстом загрузки**. Один Claude Code, но два вызова с разными промптами и данными.

```
                    Pack (один, общий — source of truth)
                   /                  \
          Creator role             Verifier role
          ├─ промпт: «создай»     ├─ промпт: «найди ошибки»
          ├─ контекст: задание     ├─ контекст: результат + эталон
          ├─ DS: рабочий репо      ├─ DS: тот же репо (read-only)
          └─ модель: Opus          └─ модель: Sonnet/Haiku
```

### Ключевые принципы

1. **Context isolation** (VR.SOTA.002): верификатор НЕ видит промежуточные рассуждения создателя
2. **Graduated verification** (VR.SOTA.003): глубина проверки пропорциональна стоимости ошибки
3. **CLAUDE.md — один**: общие правила одинаковые. Различие — в ролевых инструкциях и загружаемом контексте
4. **Pack — один**: верификатор и создатель читают одни и те же доменные знания

### Четыре роли верификации (PACK-verification)

| Роль | Что делает | Модель | Триггер |
|------|-----------|--------|---------|
| VR.R.001 Верификатор | Сравнивает с эталоном | Sonnet/Opus | После создания артефакта |
| VR.R.002 Аудитор | Проверяет полноту | Haiku/Sonnet | Стратегирование, перед релизом |
| VR.R.003 Рецензент | Экспертная оценка | Opus | Перед публикацией |
| VR.R.004 Приёмщик | Решение pass/fail | Человек | Закрытие РП, мерж в прод |

### Кросс-провайдерность

Роли могут исполняться разными агентами: Claude Code, ChatGPT, Gemini. Требование — не конкретный агент, а **загрузка правильного контекста** (эталон из Pack + ролевая инструкция).

### Связь с ОРЗ

| Стадия | Масштаб | Роли верификации |
|--------|---------|-----------------|
| Работа | Сессия | VR.R.001 на рубежах |
| Закрытие | Сессия | VR.R.001 + VR.R.004 |
| Открытие | День | VR.R.002 (аудит плана) |
| Закрытие | День | VR.R.002 + VR.R.004 |
| Стратегирование | Неделя | VR.R.002 (полный аудит) |

</details>

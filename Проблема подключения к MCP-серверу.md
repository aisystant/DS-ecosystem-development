
Отлично, значит ты хочешь восстановить связку между моим вызовом  
`"Как у меня дела за месяц?"` → `digital_twin_mcp_aisystant_workers_dev__jit_plugin.get_learner_summary`.

Сейчас вижу, что в доступных endpoint’ах MCP-инстанса есть только:

```
/mcp
/docs
```

а значит, метод `get_learner_summary` **либо не задеплоен**, либо не прописан в `manifest.json` плагина.

Чтобы починить, нужно проверить три уровня конфигурации 👇

---

### 🔧 1. Проверь декларацию эндпоинта

В `digital-twin-mcp.aisystant.workers.dev` должен быть маршрут:

```
/get_learner_summary
```

или функция с экспортом:

```ts
export const get_learner_summary = async (req) => { ... }
```

Если его нет — добавь, он должен принимать параметры вроде:

```json
{
  "user_id": "...",
  "period": "month"
}
```

и возвращать JSON с полями `summary`, `metrics`, `recommendations`.

---

### 📄 2. Обнови manifest.json MCP-плагина

В блоке `"functions"` нужно прописать новую функцию:

```json
{
  "name": "get_learner_summary",
  "description": "Возвращает сводку о прогрессе ученика за выбранный период",
  "parameters": {
    "user_id": "string",
    "period": "string"
  }
}
```

После этого перезапусти MCP endpoint (или пересобери Cloudflare Worker, если он на нём крутится).

---

### 🧩 3. Проверь экспорт в Typescript-файле плагина

Убедись, что плагин действительно экспортирует функцию под этим именем — не `getLearnerSummary`, не `fetch_learner_summary`, а именно `get_learner_summary` (оно должно совпадать с декларацией в manifest и моим namespace-вызовом).

---

Если хочешь, я могу сгенерировать шаблон функции `get_learner_summary` для MCP (на TypeScript, под Cloudflare Worker или Node) — готовый каркас с входом `prompt` и ответом в стиле твоих других функций (`{ message, history }`).

Сделать это?
